# 阶段 4 验收记录（打包与端到端验收）

> **状态**：待用户审阅（CP1）
> **时间**：2026-07-22（UTC+8）
> **计划**：`work plans/2026-07-22-14_阶段4打包与端到端验收实现计划.md`
> **选型**：`work options/2026-0722-1155_阶段4技术选型对比_打包与端到端.md`（10 项，已确认）
> **证据目录**：`.temp/阶段4验收/`（原始测量 JSON / 截图 / 清单）

---

## 1. 交付物总览（D1~D9 全交付）

| # | 交付物 | 状态 | 证据 |
|:---:|:---|:---:|:---|
| D1 | XDG 路径迁移（models/log 默认值迁契约库唯一出处）+ 选型九 | ✅ | T4.1 回归记录；paths.py/config.py |
| D1b | 托盘「设置模型目录…」+ 用户配置层 + 选型十 | ✅ | 单测 15 项；人工清单 §6 待复核 |
| D2 | onedir 构建体系（三组件 spec 模板 + tools/build.py） | ✅ | 冒烟记录（下表） |
| D3 | AppImage 封装（手写 AppDir + appimagetool） | ✅ | 三产物 `--version` 实测 |
| D4 | Launcher 正式模式目标解析联调 | ✅ | 邻接拉起 10.6s exit 0；dev/正式并行互不干扰 |
| D5 | .desktop 模板 + 幂等安装/卸载脚本 | ✅ | 安装→幂等 diff 空→卸载→再安装闭环 |
| D6 | 资产随包双环境验证 + 故障注入 | ✅ | 截图 ×3 组；图标/日志注入记录 |
| D7 | 冷启动耗时测量（T1/T2/T_total，N=5） | ✅ | t47_coldstart.json |
| D8 | 端到端验收自动化（协议 E2E 双形态 + 场景脚本 + 人工清单） | ✅/🕐 | CP0 绿；人工清单待用户逐项 |
| D9 | 收尾（README/回填/命名销账/静态检查/本记录） | ✅ | 见 §5 |

### 构建冒烟记录（onedir）

| 组件 | 体积 | AppImage 体积 | 冒烟 |
|:---|---:|---:|:---|
| service | 1536.1 MiB（torch CPU 697MiB 收编） | 476.1 MiB | ✅ --version / assets / config.yaml |
| client | 349.3 MiB | 91.0 MiB | ✅ 同上 |
| launcher | 33.7 MiB | 16.8 MiB | ✅ 同上 |

## 2. 验收标准逐条核对

### 标准 1：冷启动总耗时较旧版 66 秒显著下降（实测数据）✅

N=5（AppImage 邻接布局，缓存模型），原始数据 `.temp/阶段4验收/t47_coldstart.json`：

| 指标 | min | 中位 | P99 | max | 旧版对照 |
|:---|---:|---:|---:|---:|:---|
| T1 Socket 可连 | 0.40s | 0.60s | 0.60s | 0.60s | onefile 解压 19s → **消灭** |
| T2 模型就绪 | 10.12s | 10.62s | 12.60s | 12.68s | 在线加载 43s → **直载 10.6s** |
| T_total 编排全程 | 10.13s | 10.63s | 12.61s | 12.69s | 全流程 66s → **10.6s（-84%）** |

T3（双击→首字）为人工计时项：见人工清单 §1。T2-cold（页缓存冷）未执行：无免密 sudo，按 R7 记录缺口（补测命令见人工清单 §9）。

### 标准 2：双击 .desktop 一次拉起全套；三处图标打包环境实测 ✅/🕐

- `.desktop` 已真实安装（~/.local/share/applications/zen-vocotype.desktop + hicolor 四档），闭环测试通过
- 截图证据（打包形态）：`截图_打包客户端/`（onedir）、`截图_打包客户端_AppImage/`（AppImage）——托盘/菜单/状态色全览
- Launcher 通知图标：降级路径合规（缺失 warning 不静默，icon_loader _MEIPASS 双环境）；人眼复核见人工清单 §3
- 双击启动人眼复核：人工清单 §4（Launcher.AppImage 邻接拉起已自动化实测 10.6s）

### 标准 3：模型路径打包后实测生效（两条路径）✅

- **已就位直载**：E2E 双形态 T2 8.6s/9.0s（缓存命中）
- **空目录首启下载**：空目录 → 下载 1.2GB → 132s 就绪 → 9 样本识别重合率 1.000
  （`t48_e2e_AppImage_1784711804.json`）——旧「随包模型从未生效」事故根除
- 启动日志输出生效值与来源层（`模型目录生效值: …（来源：环境变量/用户配置文件/…）`）

### 标准 4：模型/日志/用户配置全落 XDG，挂载点零写入 ✅

- 默认值唯一出处契约库 paths.py（data/state/config/runtime 分层）；全程 AppImage 运行未触及挂载点
- 故障注入：日志不可写（`/proc/...`）→ warning + stderr 兜底 + 不崩溃（`fault_inject/` 记录）
- 用户配置仅写 `~/.config/zen_vocotype/user_config.yaml`（原子写，同目录 .tmp + replace）

### 标准 5：协议级 E2E + 场景脚本 ✅（CP0）

- 协议级 E2E（`tools/e2e_packaged.py`）：onedir 与 AppImage 双形态各 9/9 样本重合率 1.000，优雅退出 0
- 场景脚本（`tools/scenario_packaged.py`）：
  - A 幂等二次执行：首启 10.4s，二次 **0.4s** 复用无双开 ✅
  - B 崩溃回收：kill -9 服务 → 重拉就绪 10.8s exit 0 ✅
  - 慢启动由真实模型加载天然覆盖（T2 等待路径）
- 人工清单：`.temp/阶段4验收/T4.8_人工确认清单.md`（移交用户逐项）

### 标准 6：资产命名终态 ✅

- Service 图标更名 `zen_vocotype_service_icon_*`（git mv），清单 §2.1/2.2/2.4 逐项销账；
  `icon.png`（错误资产）标记不迁移
- rg 检查：分发物范围零 `grid_chat`/`star` 文件名残留（仅注释/文档中的历史教训引用，属刻意保留）

### 标准 7：自选模型目录（T4.1b）✅/🕐

- 自动化：校验三分支（不存在/不可写/挂载点内）单测 5 项；配置链优先级 4 项；损坏回退 + warning 2 项；原子写无残留 1 项
- GUI 交互人眼复核：人工清单 §6

## 3. 阶段 4 发现并修复的缺陷（实施期实测暴露）

| # | 缺陷 | 修复 |
|:---:|:---|:---|
| 1 | spec 静态分析漏收 torch/funasr/modelscope（loader 延迟 import），首轮产物 345MiB 无 ML 栈 | spec `collect_all` 显式收编（R2 落地，注释记录） |
| 2 | 自检音频路径按源码布局解析，打包形态指向产物根缺失 | loader 双环境 `_MEIPASS` 解析（与 icon_loader 同约定） |
| 3 | Launcher 邻接解析在 AppImage 内以挂载点路径为基准 | `_launcher_dir` 优先 `APPIMAGE` 环境变量（含单测固化） |
| 4 | 🔴 **幂等失效**：discovery `/proc/exe` 精确匹配遇 AppImage 挂载点路径误判 STALE → 删活锁 → 客户端双开 | 增 `APPIMAGE` 环境变量身份比对回退（单测 3 项固化） |
| 5 | 日志目录不可写时 setup_logging 直接崩溃 | 三组件 logging_setup 容错：stderr 兜底 + warning（T4.6 注入验证） |
| 6 | 场景脚本自身竞态（Launcher 返回即读子锁）导致误杀服务 | 脚本等锁存活窗口（工具缺陷，非产品缺陷） |

## 4. 与计划的两处实施偏差（均有依据并记录）

1. **Service spec 不排除 Qt**：计划 T4.2 原文假设「Service 排除 Qt 系」，与 Service 托盘（切换模型/设置模型目录…，验收标准 7 依赖）事实冲突——按既有托盘事实修正，spec 注释记录
2. **targets.py 邻接名回填**：onedir 邻接名由占位 `Zen_VocoType_Service` 回填为真产物布局 `zen_vocotype_service/zen_vocotype_service`（解析逻辑零改动，补 onedir 邻接单测）

## 5. 收尾项

- ✅ 根 README 新建「打包产物使用说明」节（布局/安装脚本/离线模型/XDG 分层/故障排查）；三组件 README 同步；常用命令.md 打包节重写
- ✅ `model_ready_timeout_s` 终值确认 180s（缓存 P99 12.6s、首启下载 132s 均覆盖），Launcher 注释 + 阶段 3 选型文档二次回填
- ✅ 静态检查：零 cwd 相对解析、零系统临时目录引用（scenario 脚本 /tmp 回退已改 ~/.local/run）、零硬编码绝对路径
- ✅ 选型文档（10 项）状态转「已确认」
- ✅ 最终回归：Protocol 36 / Service 70 / Client 128 / Launcher 104 全绿

## 6. 已知缺口（不阻塞验收）

1. T2-cold（drop_caches）未执行：无免密 sudo（R7 预案，补测命令在人工清单 §9）
2. T3 首字耗时、双击启动、通知图标、托盘 GUI 交互属人眼环节：收敛于人工清单（选型八边界）
3. Service 8 项 slow 单测（真实模型加载）默认 deselect，与阶段 1~3 基线一致；打包形态等价路径已由 E2E 脚本覆盖

---

*创建于 2026-07-22 (UTC+8)*
