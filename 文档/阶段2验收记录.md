# 阶段 2 验收记录（Zen_VocoType_Client）

> **时间**：2026-07-21 16:55 (UTC+8)
> **依据**：[`work plans/2026-0721-1534_阶段2客户端实现计划.md`](../work%20plans/2026-0721-1534_阶段2客户端实现计划.md)（已确认）
> **执行基准**：[`work options/2026-0721-0816_阶段2技术选型对比_Client客户端.md`](../work%20options/2026-0721-0816_阶段2技术选型对比_Client客户端.md) 9 项选型

---

## 1. 验收标准逐条核对（大纲 §6 阶段 2 原文）

| # | 验收标准 | 结果 | 证据 |
|:---:|:---|:---:|:---|
| 1 | 按住说话→松开出字全流程可用 | 🟢 | E2E `test_press_record_release_output_cycle`：注入按住→真实录音→松开→真实服务端识别→输出流水线执行→状态归位 idle；协议级 `test_recognize_real_sample_via_client`：示例语音（`参考代码/示例语音和文字/20260721_090043.wav`）真实识别，与参考文本重合率 ≥0.5 达标 |
| 2 | 服务端未就绪时给出明确提示而非静默失败 | 🟢 | E2E 三场景：服务端缺席（灰态+「服务端未运行」通知+缺席下识别失败仍提示且状态归位）、版本不一致（断开+「协议版本不兼容」红态+通知）、模型切换中（2002→「模型切换中」专属文案） |
| 3 | 托盘图标在开发与打包环境下均正常显示 | 🟢 | 开发环境截图自查 v1/v2；打包环境（onedir）截图自检 + 就绪态联动截图（托盘区裁剪可见绿色状态点） |

## 2. 检查点结论

| 检查点 | 结论 | 记录 |
|:---:|:---|:---|
| CP0 打包最小验证 | ✅ 通过（未回退 GTK） | `.temp/阶段2验收/T2.0_打包最小验证/实测记录.md`（PySide6 6.11.1 + PyInstaller 6.21.0，双环境 RESULT=PASS） |
| CP1 双环境端到端 | ✅ 自动化部分通过；🔺 真实热键人工验证待用户执行（见 §5） | 开发环境 E2E 5 项全绿；打包环境：截图自检 exit=0、服务端联动绿态实测 |

## 3. 交付物核对（D1~D10）

| # | 交付物 | 结果 | 测试数 |
|:---:|:---|:---:|:---:|
| D1 | 打包最小验证 | 🟢 CP0 通过 | — |
| D2 | 配置扩展 + 日志基座 | 🟢 `config.py`（8 配置项+启动校验）、`logging_setup.py` | 17 |
| D3 | 状态机 | 🟢 `state_machine.py`（5 状态 8 事件集中转移表，非法转移抛 `InvalidTransition`） | 39 |
| D4 | Socket 客户端 | 🟢 `transcribe/client.py`+`worker.py`（长连复用、health 探测、版本握手、断线重连仅一次、13 错误码透传） | 15 |
| D5 | 录音 | 🟢 `recorder/recorder.py`（InputStream 回调+队列、60s 上限、流实例复用、设备缺席启动即报错） | 8 |
| D6 | 热键 | 🟢 `hotkey/`（HotkeyBackend 抽象 + PynputBackend、组合键状态机、键重复去抖） | 11 |
| D7 | 文字输出 | 🟢 `output/`（QClipboard 主+xclip 降级、pynput 粘贴主+xdotool 降级、指纹比对恢复） | 10 |
| D8 | 托盘/UI | 🟢 `tray/`（6 状态色、版本菜单首行、通知去重、无托盘降级、截图自检模式） | 12 |
| D9 | 应用装配 | 🟢 `app.py`+`main.py`（四线程事件源收敛主线程） | E2E 5 |
| D10 | 托盘图标迁移 | 🟢 `assets/zen_vocotype_client_icon_{32,64,128,256}.png`（按资产迁移清单 §2.2 命名） | 含于 D2 |

**测试合计：110 项全绿**（含 5 项真实服务端 E2E，23.4s）。

## 4. 静态检查记录（rg，2026-07-21 16:45）

| 检查项 | 结果 |
|:---|:---:|
| 业务代码无 `print` | 🟢（main.py 一处已改 logger） |
| 无 cwd 相对路径（`os.getcwd`/`Path.cwd`/`"./`） | 🟢 零匹配 |
| 无跨组件 import（service/launcher/GridChat） | 🟢 零匹配（仅注释提及旧名） |
| 业务代码无 `time.sleep` 等裸固定延迟 | 🟢 零匹配（唯一延迟 `paste_restore_delay_ms` 经 QTimer 调度注入） |

**评审检查单落实**：音频回调仅入队/计数/置 Event（`recorder._audio_callback`）；热键回调仅 `Signal.emit`（`pynput_backend._handle_*`）；状态机仅主线程持有（`app.py` 装配）；重连仅一次（`worker._ensure_connected` 循环上限 2）；非法转移抛异常（39 项参数化测试遍历全部未定义组合）。

## 5. 遗留事项

| # | 事项 | 处置 |
|:---:|:---|:---|
| 1 | 🔺 **真实热键人工验证**：E2E 以测试钩子注入等价事件，真实 `<ctrl>+<alt>+o` 按住说话→松开出字需用户在本机实测（真实粘贴 Ctrl+V 亦属人工验证，E2E 中以 fake paster 替身避免向焦点窗口注入按键） | 用户 CP1/CP2 审阅时执行 |
| 2 | 打包形态下日志目录落于产物目录内（`dist/.../logs`，component_root 源码布局推算的已知限制） | 阶段 4 打包方案定稿时处理（日志路径策略） |
| 3 | 剪贴板 v1 仅备份恢复文本域（图像等富 MIME 不备份） | README 故障排查节已声明；后续版本评估 |
| 4 | 无托盘真实最小化 WM 实测未执行（本机 KDE 托盘可用） | 计划 R5 附加验证项，环境可得时执行；降级路径已有桩测试覆盖 |
| 5 | 默认热键两次避让（`<ctrl>+`` 旧版占用 → `<ctrl>+<alt>+v` 本机占用 → `<ctrl>+<alt>+t` 亦占用 → 定稿 `<ctrl>+<alt>+o`） | 已写入 config 注释与 README 故障排查节 |

## 6. 过程偏差记录（相对计划）

1. 用户中途新增需求并已落地：① 托盘右键菜单首行版本显示（`Zen_VocoType v0.2.0`，与旧 GridChat 区分）；② `--screenshot` 托盘布局截图自查模式（查看→验证→修改→再验证循环，已执行 v1/v2/打包三轮）
2. 计划现状盘点修正：阶段 1 服务端已于计划创建前完成（提交 `1a78ecf`），T2.3 无桩期、R4 消除（计划文档已同步修订）
3. 热键模块发现 pynput `HotKey.parse` 与 Listener 事件对特殊键（F9）的表示不一致（KeyCode(vk) vs Key 枚举），`_canonical_key` 规范化修复（测试固化）

---

*创建于 2026-07-21 16:55 (UTC+8)*
