# 阶段 3 验收记录（Zen_VocoType_Launcher）

> **⚠️ 归档状态**：本文档撰写于 2026-0722-1335，记录当时的实施状态。
> 文档中提及的"已知问题"和"待办项"可能已在后续修复，请勿以本文档推断当前代码。

> **时间**：2026-07-22 13:35 (UTC+8)
> **依据**：[`work plans/2026-07-22-12_阶段3启动器实现计划.md`](../../work%20plans/2026-07-22-12_阶段3启动器实现计划.md)
> **执行基准**：[`work options/2026-0722-1152_阶段3技术选型对比_Launcher启动器.md`](../../work%20options/2026-0722-1152_阶段3技术选型对比_Launcher启动器.md) 10 项选型（含实施期两处实测修订）

---

## 1. 验收标准逐条核对（大纲 §6 阶段 3 原文）

| # | 验收标准 | 结果 | 证据 |
|:---:|:---|:---:|:---|
| 1 | 双击桌面入口一次成功拉起全套（本阶段以命令行执行等价验证，桌面入口属阶段 4） | 🟢 | dev 模式真机：`main.py --dev` 一次执行拉起两端，退出码 0，总耗时 8.0s；正式模式目标解析逻辑单测覆盖（真二进制属阶段 4 回填联测） |
| 2 | 重复执行幂等 | 🟢 | 二次执行：discovery 幂等命中两端（pid 精确匹配），零拉起，就绪确认后退出码 0，总耗时 0.0s |
| 3 | 模拟服务端慢启动/崩溃场景下行为正确（不误判、不留孤儿进程） | 🟢 | 崩溃：编排期 `kill -9` Service → 0.2s 内快速失败（🔴 未干等超时），退出码 3，进程组无孤儿（`os.kill(pid,0)` 验证）；慢启动：模型加载 8s 自然场景预算内成功；超时路径：预算收紧 0.5s → 阶段二超时 + SIGTERM 回收干净 + 退出码 3 |
| 4 | `--dev` 模式与正式模式行为一致、互不干扰 | 🟢 | dev 实例运行中执行正式模式：独立 Launcher 锁未被阻塞，目标解析失败退出码 5（二进制属阶段 4），dev 两端不受影响；dev/正式 Socket/三方锁文件全分离（契约库常量） |

## 2. 检查点结论

| 检查点 | 结论 | 记录 |
|:---:|:---|:---|
| CP0 七场景 fake 单测 | ✅ 通过（12 项全绿） | `tests/test_t38_orchestrator.py`：成功/幂等命中/锁冲突/慢启动/编排期崩溃回收/Client 失败回收（含既有实例不越权终止分支）/Socket 外部占用 |
| CP1 真机验收 | ✅ 通过 | 本记录 §1/§3 全部实测证据 |

## 3. 交付物核对（D1~D10）

| # | 交付物 | 结果 | 测试数 |
|:---:|:---|:---:|:---:|
| D1 | 配置扩展 + 日志基座 + 图标迁移 | 🟢 `config.py`（6 新配置项+启动校验）、`logging_setup.py`、`assets/zen_vocotype_launcher_icon_{32,64,128,256}.png`、`icon_loader.py` | 23 |
| D2 | 客户端实例锁补齐 | 🟢 契约库 `CLIENT_LOCK_PATH` 等 4 常量 + Client `instance_lock.py`（二实例退出码 5）+ Service dev 锁选择 | 13 |
| D3 | 单实例锁 `locks.py` | 🟢 flock + JSON 元信息、正式/dev 分离、死亡自动释放 | 9 |
| D4 | 子进程管理 `processes.py` | 🟢 Popen 独立会话、killpg 整组回收（孙进程不逃逸实测）、两段式终止 | 11 |
| D5 | 实例识别 `discovery.py` | 🟢 PID+`/proc/exe` 精确匹配三分支、Socket 兜底探测三分支（🔴 不 unlink 他人 Socket） | 14 |
| D6 | 就绪等待 `readiness.py` | 🟢 极简协议客户端（health/ready+版本握手）、两阶段条件等待、进程死亡快速失败 | 11 |
| D7 | 通知 `notify.py` | 🟢 notify-send 三类时机 + 迁移图标、缺席降级 warning | 5 |
| D8 | 编排器 + 退出码 | 🟢 `orchestrator.py`（ExitStack 逆序清理、拉起即退出）、`exit_codes.py`（0/2/3/4/5/6） | 12 |
| D9 | `--dev` 模式 | 🟢 `targets.py`（三处差异替换唯一位置）+ `main.py` CLI 单一出口 | 10 |
| D10 | 验收实测 + README + 本记录 | 🟢 见 §1/§4/§5 | — |

**测试合计：四组件全绿** —— Launcher 99 / 契约库 26 / Client 128（T3.2 改动后回归）/ Service 65。

## 4. 实测数据

### 4.1 P99 冷启动（dev 模式，缓存模型，N=5）

| 次数 | 退出码 | 总耗时 |
|:---:|:---:|:---:|
| 1 | 0 | 7.66s |
| 2 | 0 | 8.54s |
| 3 | 0 | 7.93s |
| 4 | 0 | 8.29s |
| 5 | 0 | 8.16s |

**P99 ≈ 8.5s，较旧版 66s 下降 87%**（已回填选型文档 §3；`model_ready_timeout_s` 180s 保留覆盖首次模型下载）。

### 4.2 关键场景实测（2026-07-22 13:07~13:20 真机）

| 场景 | 结果 |
|:---|:---|
| 一次拉起全套 | 退出码 0，两端存活（Launcher 退出后 `ps` 验证 pid 493245/493466） |
| 幂等二次执行 | 零拉起，退出码 0，0.0s |
| 运行中 Service `kill -9` 后恢复 | 陈旧锁清理 + Socket 残留识别 + 重拉成功，退出码 0 |
| 编排期 `kill -9` Service | 0.2s 快速失败（进程死亡检测），退出码 3，无孤儿 |
| 超时预算收紧 0.5s | 阶段二超时 + SIGTERM 回收，退出码 3（无管道实测） |
| dev 实例中执行正式模式 | 互不干扰，退出码 5（目标缺失提示明确） |

## 5. 静态检查记录（rg，2026-07-22 13:25）

| 检查项 | 结果 |
|:---|:---:|
| 业务代码无 `print` | 🟢（main.py 配置非法分支已改 loguru 默认 sink） |
| 无 cwd 相对路径 | 🟢 零匹配 |
| 无跨组件 import | 🟢 零匹配（仅 docstring 提及组件名） |
| 无固定 sleep | 🟢 仅两处条件等待轮询间隔（命名常量 `ready_poll_interval_ms`/`_TERMINATE_POLL_INTERVAL_S`，符合 C1） |

## 6. 实施期修订记录（选型文档已同步）

| # | 修订 | 原因 |
|:---:|:---|:---|
| 1 | **PDEATHSIG 保险废弃**（选型五方案 C 排除） | 与选型七方案 A 根本冲突：Launcher 正常退出触发子进程父死信号，两端被 SIGTERM 带走（首次 dev 实测 SIGTERM 时间戳与 Launcher 退出吻合）。残余风险由「组件锁文件 + 幂等收养」覆盖 |
| 2 | **P99 回填**（选型二超时依据） | 实测 8.5s ≪ 预算，180s 保留覆盖首次下载 |

## 7. 遗留项（移交阶段 4）

1. 正式模式真二进制（AppImage/onedir）联测——阶段 4 打包产物回填后执行协议级 E2E
2. `.desktop` 桌面入口双击验证（验收标准 1 的原始形态）——阶段 4
3. 首次模型下载场景的 180s 预算实测——阶段 4 模型分发验收时覆盖

---

*创建于 2026-07-22 13:35 (UTC+8)*
