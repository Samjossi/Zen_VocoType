# 阶段 0 设计定稿 验收记录

> **⚠️ 归档状态**：本文档撰写于 2026-0721-0845，记录当时的实施状态。
> 文档中提及的"已知问题"和"待办项"可能已在后续修复，请勿以本文档推断当前代码。

> **状态**：待审阅（CP2，合并 CP1 协议文档审阅）
> **范围**：重写大纲 §6 阶段 0 全部交付物
> **时间**：2026-07-21 08:45 (UTC+8)（执行完毕）
> **执行依据**：`文档/修改记录/2026-0721-0805_阶段0设计定稿计划.md`

---

## 1. 交付物核对（D1~D5）

| # | 交付物 | 落盘位置 | 状态 |
|:---:|:---|:---|:---:|
| D1 | 通信协议设计文档 | `文档/通信协议设计_v1.0.md` | 🟢 待 CP1 审阅 |
| D2 | 协议契约库模块划分与接入方式 | `Zen_VocoType_Protocol/`（pyproject + 5 模块骨架，editable install 成功） | 🟢 |
| D3 | 三组件目录结构与配置方案 | 三组件 `pyproject.toml` + `config.yaml` + `src/<包>/config.py` + README 结构说明 | 🟢 |
| D4 | 客户端 UI 技术选型结论 | 选型文档定 PySide6；本机托盘冒烟 **True**（见 §3） | 🟢 |
| D5 | 资产迁移清单与重命名方案 | `文档/资产迁移清单_v1.0.md`（品牌图标结论：沿用旧图标占位） | 🟢 |

收尾动作：选型 8 项结论已回填重写大纲（大纲与选型文档状态均转为「已确认」，阶段 1/2/4 交付物描述按选型修订）。

## 2. 任务执行摘要（T0.1~T0.6）

| 任务 | 结果 |
|:---|:---|
| T0.1 环境基座 | 基线依赖核实已就位（PySide6 6.11.1 / pynput 1.8.2 / sounddevice 0.5.5 / pydantic-settings 2.14.2），补装 pytest 9.1.1（dev 组）；FunASR/torch/modelscope/PyInstaller 此前已装，阶段 1 可直接用 |
| T0.2 契约库骨架 | 清理残留 pycache；新建 `version/actions/errors/paths/frames` 五模块骨架；`uv pip install -e` 成功（覆盖历史残留 1.0.0 安装为 0.1.0）；全模块导入冒烟通过 |
| T0.3 协议设计文档 | 复合帧格式、6 个 action、13 个错误码、版本握手、就绪语义定稿；常量与契约库骨架同源（错误码含审查修复新增的 1005/1006，见 §5） |
| T0.4 三组件骨架 | 清理残留 pycache；各组件 `pyproject.toml`（独立项目 + 依赖声明 + 契约库依赖）；`config.yaml` + `Settings` 单一配置源；客户端五子包（hotkey/recorder/transcribe/output/tray）；README 同步结构说明 |
| T0.5 资产清单 | 旧资产实物盘点全部在册；重命名映射表定稿；品牌图标结论：沿用旧图标占位，另行立项 |
| T0.6 回填 | 大纲/选型文档转「已确认」；阶段 1/2/4 交付物描述按选型修订 |

## 3. 冒烟实测数据

| 验证项 | 结果 |
|:---|:---:|
| `import PySide6` | 🟢 |
| `QSystemTrayIcon.isSystemTrayAvailable()`（X11 / ubuntu:GNOME / xcb） | 🟢 **True** |
| `QSystemTrayIcon.supportsMessages()` | 🟢 True |
| 契约库 editable install + 全模块导入 | 🟢 |
| 版本兼容判定 `is_compatible("1.0.0","1.0.1")=True`、`("1.0.0","1.1.0")=False` | 🟢 |
| 三组件 `Settings` 加载（YAML + 代码默认值） | 🟢 |
| 环境变量覆盖（`ZEN_VOCOTYPE_CLIENT_HOTKEY`） | 🟢 |
| 三组件零交叉 import 静态检查（rg） | 🟢 无命中 |
| 默认 Socket 路径解析（`$XDG_RUNTIME_DIR` → `/run/user/1000/zen_vocotype.sock`） | 🟢 |
| 三端 yaml 不再覆盖默认路径（唯一出处回落契约库） | 🟢 |

详细记录：`.temp/阶段0验收/T0.1_环境基座冒烟记录.md`（含冒烟脚本 `smoke_tray.py`）。

## 4. 边界遵守情况

- 🟢 未写任何业务实现代码（契约库 frames 为 NotImplementedError 骨架；三组件 main.py 为明确报错退出的骨架）
- 🟢 未做 Qt+PyInstaller 打包验证（已按选型 §11-3 排入阶段 2 早期，并写入大纲阶段 2 交付物）
- 🟢 未实际复制图标资产（仅清单与方案）
- 🟢 全程使用项目 `.venv`；临时文件均在项目内 `.temp/`

## 5. 偏差说明

1. **CP1 合并**：计划原定 T0.3 后暂停待 CP1 审阅；按用户「接着按计划来走」的指令连续执行，CP1（协议设计文档）与本 CP2 合并审阅
2. **依赖登记**：计划预期向 venv 安装基线依赖，实测此前已全部装好，仅补 pytest；各组件依赖以 `pyproject.toml` 声明为准（monorepo 同 venv 开发形态）
3. **审查修复（2026-07-21 二轮）**：分支审查发现 5 项 WARNING + 2 项 SUGGESTION，已全部修复并复测通过——
   - Socket 默认路径迁出 /tmp：`$XDG_RUNTIME_DIR`（回退 `~/.local/run`），协议文档新增 §7.1 Socket 本地访问控制强制项（符号链接/属主校验、chmod 0600、`SO_PEERCRED` 同 UID 校验）
   - 错误码 1004 双语义拆分：新增 `1005 ERR_ACTION_NOT_SUPPORTED`、`1006 ERR_UNAUTHORIZED_PEER`；`ALL_ACTIONS` 注释写明 audio_chunk 放行—兜底机制
   - 三端 config.yaml 的 socket 路径改为注释掉的覆盖示例，默认值唯一出处真正回落 `paths.py`
   - 配置行为逻辑（优先级元组 + 组件根推算）迁入契约库 `settings.py` 公共基类，三组件继承
   - 删除服务端 config.py 未使用的 `Field` 导入；`component_root` 标注打包形态限制（阶段 1 resource_locator 职责）

## 6. 待审阅事项（请用户确认）

1. `文档/通信协议设计_v1.0.md` 全文（CP1）——重点：复合帧头字段表、错误码区间、`ready` 语义、版本握手严格策略
2. `文档/资产迁移清单_v1.0.md`——重点：重命名映射、品牌图标「沿用占位」结论
3. 契约库骨架常量（actions/errors/paths 取值）
4. 三组件骨架结构与 `config.yaml` 配置项命名

审阅通过后：本文档与两份设计文档状态转「已确认」，阶段 0 关闭，进入阶段 1（Zen_VocoType_Service 实现）。

---

*创建于 2026-07-21 08:45 (UTC+8)*
