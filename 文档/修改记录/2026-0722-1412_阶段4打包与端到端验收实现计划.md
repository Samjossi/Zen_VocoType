> **⚠️ 归档状态**：本文档撰写于 2026-0722-1412，记录当时的实施状态。
> 文档中提及的“已知问题”和“待办项”可能已在后续修复，请勿以本文档推断当前代码。

# 阶段 4 打包与端到端验收 工作计划

> **状态**：执行完毕（CP1 待用户审阅；2026-07-22 同日实施完成）
> **范围**：仅覆盖重写大纲 §6「阶段 4 — 打包与端到端验收」，含 2 项代码现状隐患前置修复、1 项选型文档补漏（日志路径策略）与托盘「设置模型目录」用户自选功能（2026-07-22 14:49 经用户确认纳入）
> **时间**：2026-07-22 14:12 (UTC+8)（创建；14:49 修订：增列 T4.1b 与选型十）
> **优先级**：高

---

## 1. 目标与依据

依据三份输入文档，完成阶段 4 全部交付物：

- `2026-0721-0529_GridChat迁移Zen_VocoType重写大纲.md` §6 阶段 4（已确认）
- `2026-0722-1155_阶段4技术选型对比_打包与端到端.md`（8 项选型结论作为本计划的执行基准，⚠️ 尚处草稿，本计划与其一并审阅）
- `2026-0722-1355_服务端冷启动超时致启动器判定失败的整改建议.md`（旧事故教训对照——本计划 T4.1 即其残余隐患的落地修复）

**阶段 4 交付物清单**（选型 8 项结论 + 隐患修复 + 文档补漏 + 用户自选模型目录功能）：

| # | 交付物 | 对应任务 |
|:---:|:---|:---:|
| D1 | 🔴 隐患前置修复：`models_dir`/`log_dir` 默认值迁 XDG 用户目录（契约库唯一出处）+ 选型文档补「选型九：日志路径策略」 | T4.1 |
| D1b | 托盘「设置模型目录…」功能（GUI 自选 + 用户配置持久化 + 重启生效）+ 选型文档补「选型十：用户可配置模型目录」 | T4.1b |
| D2 | PyInstaller onedir 构建体系（三组件 spec 模板 + `tools/build.py` 统一入口） | T4.2 |
| D3 | AppImage 封装（手写 AppDir 模板 + AppRun + appimagetool，三独立产物 + 邻接目录布局） | T4.3 |
| D4 | Launcher 正式模式目标解析与打包产物联调（邻接约定回填真二进制） | T4.4 |
| D5 | `.desktop` 模板 + 幂等安装/卸载脚本（hicolor 图标集成） | T4.5 |
| D6 | 资产随包双环境验证 + 图标缺失故障注入测试 | T4.6 |
| D7 | 冷启动耗时测量（T1/T2/T3/T2-cold，N≥5 取 P99） | T4.7 |
| D8 | 端到端验收自动化（协议级 E2E + 场景脚本 + 人工确认清单） | T4.8 |
| D9 | 收尾：README 打包说明节、`model_ready_timeout_s` 回填、资产命名终态核对、阶段 4 验收记录 | T4.9 |

**验收标准**（大纲 §6 阶段 4 + 选型文档约束 C1~C8）：

1. 打包后冷启动全流程一次成功，总耗时较旧版 66 秒显著下降并有实测数据（T1/T2/T3 数据表）
2. 双击 `.desktop` 入口一次拉起全套；托盘/通知/菜单三处图标打包环境下实测可见
3. 打包后模型路径实测生效：空目录首启下载 → `ready` 就绪、模型已就位直接加载，两条路径均通过（根除旧「随包模型从未生效」事故）
4. 打包后日志/模型/用户配置全部落 XDG 用户目录，AppImage 只读挂载点零写入（T4.1 修复的验收口径）
5. 协议级自动 E2E（打包产物双端互连 + 真实音频样例识别比对）通过；场景脚本（幂等/慢启动/崩溃回收）通过
6. 资产命名终态：全部 `zen_vocotype_*`，零 `grid_chat`/`star` 残留（资产迁移清单逐项销账）
7. 托盘「设置模型目录…」可选定目录并持久化至用户配置文件，重启后生效；非法目录（不存在/不可写/AppImage 挂载点内）校验拒绝且不静默；用户配置文件损坏时回退默认值并记 warning

---

## 2. 现状盘点（2026-07-22 14:12 实测）

| 项 | 现状 | 对计划的影响 |
|:---|:---|:---|
| 🔴 `models_dir` 默认值 | `Zen_VocoType_Service/src/zen_vocotype_service/config.py:79` 为 `COMPONENT_ROOT / "models"`（程序目录内）；`MODELSCOPE_CACHE` 经 main.py 首行指向该值（T1.4 顺序红线有测试固化） | 打包后落在 AppImage **只读挂载点**，模型下载必失败——旧事故「路径错位 + 随包模型死重」同款。T4.1 改为 XDG 数据目录，默认值唯一出处迁契约库 `paths.py`；改后须回归 T1.4 顺序测试 |
| 🔴 `log_dir` 默认值 | 三组件均为 `COMPONENT_ROOT / "logs"`（Service:81 / Client:68 / Launcher:38）；阶段 2 验收记录已记「打包形态日志落产物目录」为已知限制，**但选型文档 8 项中无日志路径策略——属遗漏** | AppImage 只读挂载写日志必失败，旧事故「日志写到 cwd 继承目录」同款。T4.1 迁 XDG 状态目录 + 选型文档补「选型九」 |
| Socket 路径 | ✅ 已冻结：`$XDG_RUNTIME_DIR`（回退 `~/.local/run` 校验 0700），含符号链接/属主/`SO_PEERCRED` 校验（阶段 0/1 验收 🟢） | 无打包风险，作为 T4.1 路径 helper 的既有范式参照 |
| 模型异步加载/先监听 | ✅ 阶段 1 已验收：Socket 可连 ≤5s、`ready` 协议、模型异步加载 | T4.7 测量直接复用协议路径（选型七方案 A） |
| 路径自定位 helper | ✅ 三组件 `icon_loader.py` 已兼容 `_MEIPASS`（阶段 2 双环境实测通过）；契约库 `settings.component_root` 有打包形态说明 | T4.6 直接复用；T4.1 的 XDG helper 按同款规格实现 |
| onedir 最小验证 | ✅ 阶段 2 CP0 已完成 PySide6 + PyInstaller onedir 打包最小验证（双环境 PASS），Qt 插件收编风险已排除 | T4.2 spec 模板有其经验基础 |
| Launcher 目标解析 | ✅ 阶段 3 `targets.py` 已实现「配置显式路径 → 邻接目录约定」解析（正式模式当时以假二进制桩验证） | T4.4 仅需以真产物回填联测，🔴 禁止重写解析逻辑 |
| 构建工具目录 | `tools/` 已存在（`sync_versions.py`），符合选型一「构建脚本集中放 `tools/`」约定 | T4.2 在其内新增 `build.py` |
| 模型下载机制 | ✅ Service 已有 `model_download` action 与 modelscope 缓存机制（阶段 1/2 验收） | T4.8 首启下载用例直接驱动既有 action |
| 版本管理 | `versions.toml` 统一 1.0 + 一致性测试 | 契约库改动（T4.1）后跑一致性测试 |
| `model_ready_timeout_s` | 阶段 3 初值 180s（dev 实测 P99 8.5s 已回填一次）；打包形态数据缺失 | T4.7 以打包产物 P99 二次回填校准 |
| Service 托盘菜单 | ✅ PySide6 实现，已有「切换模型►」「打开日志目录」等项（`tray/tray.py`），Qt 文件对话框可用 | T4.1b 在其上扩展「设置模型目录…」项，零新 UI 框架引入 |
| 用户配置文件层 | 🔴 不存在：当前配置链仅「包内 config.yaml 默认 → 环境变量覆盖」，无运行时用户配置持久化层 | T4.1b 新增用户配置文件（XDG config 目录）作为覆盖链新层；🔴 禁止写包内 config.yaml（AppImage 只读） |

---

## 3. 任务清单

> 负责人：AI 执行，用户审阅。预计总耗时：3~4 个工作日。
> 任务顺序按依赖关系排列，🔺 为检查点（Checkpoint）。

### T4.1 🔴 隐患前置修复：XDG 路径迁移 + 选型文档补「选型九」　【高】【预计 2.5h】

> 依据：旧事故整改建议文档 §3.1/§3.2 同款隐患（路径错位）在当前代码尚未落地修复；阶段 2 验收记录遗留「日志路径策略待阶段 4 定稿」。

- **内容**：
  1. 契约库 `paths.py` 增补（与 Socket 路径同注释规格、唯一出处）：
     - `DEFAULT_MODELS_DIR`：`$XDG_DATA_HOME/zen_vocotype/models`（回退 `~/.local/share/zen_vocotype/models`）
     - `DEFAULT_LOG_DIR`：`$XDG_STATE_HOME/zen_vocotype/logs`（回退 `~/.local/state/zen_vocotype/logs`）
     - 目录自动创建 + 权限校验；🔴 禁止程序目录内、🔴 禁止 cwd 相对、🔴 禁止系统临时目录
  2. Service `config.py`：`models_dir` 默认值改引 `DEFAULT_MODELS_DIR`；`log_dir` 默认值改引 `DEFAULT_LOG_DIR`（Service 子目录如 `logs/service/` 或按组件分文件，实施时定稿并注释依据）
  3. Client / Launcher `config.py`：`log_dir` 默认值同款换源
  4. 回归验证：T1.4 `MODELSCOPE_CACHE` 导入顺序测试（子进程实测）必须保持绿；三组件既有测试全量回归；`test_version_consistency` 通过
  5. Service 托盘「打开日志目录」功能对新路径实测（开发环境）
  6. **选型文档补漏**：在 `文档/选型记录/2026-0722-1155_阶段4技术选型对比_打包与端到端.md` 增补「选型九：运行时数据与日志路径策略」一节（背景/方案对比/推荐结论：XDG 分层——模型 data、日志 state、配置 config、运行 runtime；汇总表同步加第 9 行），并修订 §11 实施要点
  7. README 同步：三组件 README 配置表中 `models_dir`/`log_dir` 默认值描述更新
- **产出**：契约库 `paths.py` 增量 + 三组件 `config.py` 默认值换源 + 回归记录 + 选型文档「选型九」节 + README 更新
- **依赖**：无（🔴 必须最先做，后续打包任务全部建立在其上）
- **红线**：🔴 改动面仅限路径默认值与契约库增量，禁止顺手重构业务逻辑；用户显式配置的覆盖能力不得受影响（pydantic-settings 环境变量/config.yaml 覆盖链保持）

### T4.1b 托盘「设置模型目录…」：用户自选 + 配置持久化　【高】【预计 2.5h】

> 依据：2026-07-22 14:49 用户确认纳入。价值排序：T4.1 的 XDG 默认值是「不错」（开箱即用不踩坑），本任务的 GUI 自选是「更好」（进阶用户控制权——外置盘、复用已有 modelscope 缓存、离线手工放置场景），两者为地基与装修关系，实施顺序不得颠倒。

- **内容**：
  1. 契约库 `paths.py` 增 `DEFAULT_USER_CONFIG_PATH`：`$XDG_CONFIG_HOME/zen_vocotype/user_config.yaml`（回退 `~/.config/zen_vocotype/user_config.yaml`），唯一出处同规格注释
  2. 配置链扩展：组件默认值（契约库）→ 包内 `config.yaml` → **用户配置文件** → 环境变量；用户配置文件🔴 禁止写包内（AppImage 只读挂载——写包内配置即下一个「路径失效事故」）；读写用原子写（临时文件 + rename，🔴 禁止系统临时目录——用同目录临时文件）；文件损坏/解析失败时回退默认值并记 warning（🔴 禁止静默丢弃、禁止崩溃）
  3. Service 托盘菜单新增「设置模型目录…」项（位置：「切换模型►」附近、「打开日志目录」之上；菜单区或悬停提示展示当前生效目录）：`QFileDialog.getExistingDirectory` 选目录 → 校验（存在性 / **可写**——不可写则只能加载不能下载须明确警告 / 🔴 拒绝 AppImage 挂载点内路径）→ 校验通过写用户配置文件 → 提示「将于下次启动生效」；校验失败不保存并告知原因（不静默）
  4. 生效语义 v1 定为「**保存后重启生效**」：因 `MODELSCOPE_CACHE` 顺序红线（main.py 首行设置、T1.4 测试固化），运行期改目录无法对已加载模型生效；运行期热切（重设 env + 模型重载）属 v2 增强，本阶段不做
  5. 切换后目录为空的行为：走既有 `model_download` 通道下载或提示手工放置（选型四离线补救路径）；🔴 v1 不做旧目录模型迁移/复制，README 文档化
  6. **选型文档补漏**：在选型文档增补「选型十：用户可配置模型目录」一节（背景/方案对比——GUI 自选持久化 / 仅环境变量 / 仅手改 YAML；推荐结论；汇总表加第 10 行）
  7. 单元测试：用户配置读写与损坏回退、目录校验三分支（不存在/不可写/挂载点内）、配置链优先级（用户配置 > 包内默认、环境变量 > 用户配置）、T1.4 顺序测试保持绿、原子写不产生残留
- **产出**：契约库 `paths.py` 增量 + 用户配置层模块 + 托盘菜单项与校验逻辑 + 选型文档「选型十」节 + 测试记录
- **依赖**：T4.1（XDG 路径 helper 与用户配置目录概念先行）
- **红线**：🔴 持久化仅写用户配置文件；🔴 生效语义仅「重启生效」，不碰 MODELSCOPE_CACHE 顺序红线；菜单交互失败全程有日志与 warning，零静默

### T4.2 onedir 构建体系：三组件 spec 模板 + `tools/build.py`　【高】【预计 3h】

- **内容**：
  1. 三组件 `.spec` 文件（选型一方案 A）：结构同构、共用模板段（协议契约库收编、`assets/` 经 `datas` 收编、`config.yaml` 默认配置随包）；差异仅入口/名称/资产
  2. spec 显式排除项：Service 排除 Qt 系、Client/Launcher 排除 torch/FunASR 系，控制产物体积
  3. `tools/build.py`：统一入口 `--component service|client|launcher [--appimage]`；产物输出至项目内 `dist/`（C8）；🔴 禁止各组件目录内再长私有打包脚本
  4. 构建后冒烟检查：产物存在、二进制可执行、`--version` 探针可跑（🔴 未过冒烟不进入封装）
  5. 构建临时产物全部落 `./.temp/`（C8，🔴 禁止系统临时目录；PyInstaller `workpath`/`--distpath` 显式指定）
  6. 冒烟记录：三组件 onedir 产物体积、文件清单抽查（协议库/资产/config.yaml 已收编）
- **产出**：三组件 spec + `tools/build.py` + 冒烟检查记录
- **依赖**：T4.1

### T4.3 AppImage 封装：手写 AppDir + appimagetool　【高】【预计 2.5h】

- **内容**：
  1. AppDir 模板（选型二方案 A）：`AppRun`（`_APPDIR` 自定位 exec onedir 入口二进制，🔴 禁止 cwd 相对）+ `.desktop` + hicolor 图标按规范摆放；纯封装🔴 不做二次依赖收编
  2. `tools/build.py --appimage` 集成：onedir 拷入 AppDir → appimagetool 打包 → 输出 `dist/Zen_VocoType_{Service,Client,Launcher}.AppImage`
  3. 三独立 AppImage + 邻接目录布局验证（选型三）：三份产物放同一目录，AppImage 双击/命令行各自可跑
  4. appimagetool 缺席时明确报错提示安装方式（🔴 禁止静默跳过封装）
  5. 保留 onedir 裸产物作为二级分发物（高级用户/调试）
- **产出**：AppDir 模板 + 封装集成 + 三份 AppImage 产物 + 布局验证记录
- **依赖**：T4.2

### T4.4 Launcher 正式模式目标解析联调　【高】【预计 1h】

- **内容**：
  1. 以真产物回填阶段 3 `targets.py` 的正式模式解析（🔴 复用既有逻辑，不重写）：同目录 AppImage → 同目录 onedir 目录 → 配置显式路径，缺失报错退出码 5
  2. 联测：邻接布局下 Launcher.AppImage 拉起 Service/Client AppImage 全流程（dev 模式行为不回归）
  3. 双向隔离回归：正式实例运行中 dev 模式互不干扰（阶段 3 语义保持）
- **产出**：联测记录（含拉起耗时初值，供 T4.7 对照）
- **依赖**：T4.3

### T4.5 `.desktop` 模板 + 幂等安装/卸载脚本　【高】【预计 2h】

- **内容**：
  1. `.desktop` 模板（选型五方案 A）：`Exec=`/`Icon=` 占位符，🔴 仓库内零绝对路径硬编码（旧 `grid_chat.desktop` 脱节教训）
  2. 安装脚本（`tools/install_desktop.py` 或 shell，实施时定稿）：按实际摆放路径渲染模板 → 写入 `~/.local/share/applications/`；图标安装至 `~/.local/share/icons/hicolor/<size>/apps/zen-vocotype.png`（多尺寸来自 `zen_vocotype_launcher_icon_{32,64,128,256}.png`）；`update-desktop-database` 容错；幂等（重复执行结果一致）、纯用户态（🔴 无需 root）
  3. 卸载脚本一并提供（删除 .desktop + hicolor 图标，幂等）
  4. 测试：安装→渲染路径正确→重复安装 diff 为空→卸载→再安装闭环
- **产出**：模板 + 安装/卸载脚本 + 闭环测试记录
- **依赖**：T4.3（图标与产物就位）

### T4.6 资产随包双环境验证 + 故障注入　【高】【预计 1.5h】

- **内容**：
  1. 打包环境下三处用图逐一截图留证（选型六）：Client 托盘、Launcher 通知、`.desktop` 应用菜单图标
  2. 故障注入测试：人为移除/改名包内图标文件 → 确认 warning 日志 + 降级行为符合预期（🔴 禁止静默 None）；日志不可写场景同款注入（T4.1 验收口径：不崩溃、stderr 兜底、记 warning）
  3. onedir 与 AppImage 两种形态各跑一遍（🔴 禁止只在开发环境验收，C3）
- **产出**：截图证据 + 故障注入记录
- **依赖**：T4.4、T4.5

### T4.7 冷启动耗时测量（T1/T2/T3/T2-cold）　【高】【预计 2h】

- **内容**：
  1. Launcher 日志结构化输出耗时字段（选型七方案 A）：拉起时刻 → T1 Socket 可连 → T2 `ready` 模型就绪；T3 端到端首字（双击入口 → 首次按住说话出字）人工计时配合日志
  2. 每指标 N≥5 次取 P99/中位数；T2-cold 页缓存冷场景（`echo 3 > drop_caches` 后，需 root 授权一次）实测记录不设硬预算
  3. 原始数据存 `.temp/阶段4验收/`（C8）；🔴 禁止只报单次最好成绩
  4. 与旧版 66 秒对比表（19s 解压 + 43s 在线加载 + 时序误判 逐项对照消灭证据）
- **产出**：耗时数据表 + 原始测量数据
- **依赖**：T4.4

### T4.8 端到端验收自动化　【高】【预计 2.5h】　🔺

- **内容**：
  1. **协议级自动 E2E**（选型八层次 1）：以打包产物拉起 Service + Client 无头逻辑层，复合帧 `recognize` 真实音频样例识别比对（复用阶段 2 E2E 资产与示例语音）；onedir 与 AppImage 两形态各跑
  2. **模型首启用例**（验收标准 3）：模型目录为空 → `model_download`/首次切换触发下载 → `ready` 就绪全流程；模型已就位 → 直接加载——两条路径均实测
  3. **场景脚本**（层次 2）：Launcher 幂等二次执行、模拟慢启动、模拟 Service 崩溃回收（复用阶段 3 fake 机制，打包版跑冒烟子集）
  4. **人工确认清单**（层次 3）：真实按住热键出字、托盘 6 状态色抽查、通知图标、应用菜单图标与双击启动、模型下载首启流程——清单逐项 🟢/🔴 留证
- **产出**：E2E 脚本与结果 + 场景脚本记录 + 人工清单（移交用户逐项确认）
- **依赖**：T4.4、T4.5
- **检查点 CP0**：协议级 E2E + 场景脚本全绿记录提交用户确认后，才进入人工清单与收尾

### T4.9 收尾：README + 回填 + 命名终态核对 + 阶段 4 验收记录　【高】【预计 2h】　🔺

- **内容**：
  1. 三组件 README 增/更新「打包产物使用说明」节：下载布局（邻接目录约定）、安装/卸载脚本用法、离线模型手工放置步骤（目录结构 + 校验方式，选型四）
  2. `model_ready_timeout_s` 以 T4.7 打包形态 P99 数据回填校准（Launcher 配置默认值 + 选型文档同步）
  3. 资产命名终态核对：全仓 rg 检查零 `grid_chat`/`star` 残留（分发物范围内），`文档/资产迁移清单_v1.0.md` 逐项销账（大纲 §7-5 最终确认）
  4. 静态检查：无 cwd 相对路径、无系统临时目录引用、无包内绝对路径硬编码（rg 检查记录）
  5. 撰写 `文档/阶段4验收记录.md`，对照验收标准 1~6 逐条核对（含 T1/T2/T3 数据表、三处图标截图、E2E 结果、人工清单结论）
  6. 选型文档状态「草稿 → 已确认」（与本计划验收一并提交）
- **产出**：README 更新 + 回填记录 + 命名核对记录 + 阶段 4 验收记录
- **依赖**：T4.6、T4.7、T4.8
- **检查点 CP1**：验收记录提交用户审阅，通过后阶段 4 关闭

---

## 4. 里程碑与检查点

```
T4.1（隐患修复+选型九）──► T4.1b（托盘设置模型目录+选型十）──► T4.2（spec+build.py）──► T4.3（AppImage）──► T4.4（目标解析联调）─┬─► T4.5（desktop）─┐
                                                                                                                              ├─► T4.7（耗时测量） ├─► T4.9 ──►[CP1 阶段4验收]
                                                                                                                              └─► T4.6（资产验证）─┤      ▲
                                                                                    T4.8（E2E+场景脚本）──►[CP0]────────────────┘
```

| 检查点 | 内容 | 审阅人 |
|:---:|:---|:---:|
| CP0 | 协议级自动 E2E（双形态）+ 场景脚本（幂等/慢启动/崩溃回收）全绿记录 | 用户 |
| CP1 | 阶段 4 验收记录（验收标准 1~6 逐条 + T1/T2/T3 数据表 + 图标截图 + 人工清单结论） | 用户 |

---

## 5. 边界声明（本计划明确不做的事）

1. 🔴 不做 zsync 增量更新（选型二已定，v2 评估）
2. 🔴 不做独立模型包分发线（选型四已定；仅文档化离线手工放置补救）
3. 🔴 不做系统包（deb/rpm/AUR）与多发行版适配（阶段 0 已排除）
4. 🔴 不做全自动输入注入/截图像素比对（选型八已排除；人眼环节收敛为人工清单）
5. 🔴 不做驻留监控/崩溃自动重启（阶段 3 选型七方案 A 已定，v2 评估）
6. 🔴 不修改 Service/Client 业务逻辑（阶段 1/2 已验收冻结）；T4.1 的改动面仅限路径默认值与契约库增量
7. 🔴 不做 Windows/macOS 打包（本项目目标平台 Linux）
8. 🔴 不做模型目录运行期热切换（T4.1b v1 语义为「保存后重启生效」，热切换属 v2）；不做旧目录模型迁移/复制工具（文档化手工步骤）
9. 🔴 用户配置文件仅承载覆盖项（如 `models_dir`），不做全量配置 GUI 编辑器（v2 评估）

## 6. 风险与应对

| # | 风险 | 应对 |
|:---:|:---|:---|
| R1 | T4.1 改动三组件配置默认值，引入回归 | 改动面收敛为「默认值换源」；T1.4 顺序测试 + 三组件既有测试全量回归 + 版本一致性测试全绿后方可推进 T4.2；用户显式配置覆盖链不受影响 |
| R2 | Service 含 torch/FunASR，onedir 产物体积仍可能偏大、收编缺漏 hidden import | spec 显式排除 Qt 系；构建冒烟含 `--version` 探针；缺漏按 PyInstaller hook 增量补齐并记录进 spec 注释 |
| R3 | AppImage 在目标环境 FUSE 缺失无法运行 | 安装说明文档化 `--appimage-extract-and-run` 兜底与 FUSE 安装指引；onedir 裸产物作为二级分发物托底 |
| R4 | 模型首启下载耗时不可控（网络差异），影响 T2/T3 数据解读 | T2 口径限定「模型已缓存」场景；首启下载单列为 E2E 用例与人工清单项，不计入 T2 预算 |
| R5 | 目标环境无 `notify-send`/桌面环境差异导致通知/菜单图标验证受阻 | 通知缺席降级行为本身即验收点（warning 不静默）；人工清单记录环境信息（DE/版本）随验收报告留证 |
| R6 | 选型文档（草稿）审阅意见要求调整方案 | 本计划与选型文档（含 T4.1 增补的选型九）一并审阅；选型若有改动，计划对应任务在 CP1 前同步修订 |
| R7 | `echo 3 > drop_caches` 需 root，用户环境不便执行 | T2-cold 不设硬预算仅记录；无法执行时在验收报告中注明缺口与原因，不阻塞验收 |
| R8 | 用户配置文件写坏/被手工改坏，或用户选了不可用目录后模型无法加载 | 原子写防半截文件；解析失败回退默认值 + warning；托盘展示当前生效目录便于自查；Service 启动时 `models_dir` 不可写/不存在记明确错误日志（🔴 禁止静默回退在线加载——旧事故「名义在线实为缓存」的教训） |
| R9 | 用户配置层引入后配置链复杂度上升，排障困难 | 启动日志输出各配置项的最终生效值与来源层（default/包内/用户配置/环境变量）；README 故障排查节同步 |

## 7. 完成定义（DoD）

- [x] T4.1 完成：`models_dir`/`log_dir` 默认值迁 XDG 用户目录（契约库唯一出处），三组件回归全绿；选型文档补「选型九：日志路径策略」
- [x] T4.1b 完成：托盘「设置模型目录…」可选定目录、校验三分支不静默、持久化至用户配置文件、重启生效；选型文档补「选型十：用户可配置模型目录」
- [x] CP0 通过：协议级自动 E2E（onedir + AppImage 双形态）与场景脚本全绿
- [x] D1~D9（含 D1b）全部交付物实现并有测试/实测记录
- [x] 验收标准 1：T1/T2/T3 实测数据表（N≥5，P99/中位数），总耗时较旧版 66 秒显著下降
- [~] 验收标准 2：双击 .desktop 拉起全套（自动化实测）；三处图标打包环境截图留证（已生成，人眼复核在人工清单）
- [x] 验收标准 3：模型空目录首启下载→就绪、已就位直接加载，两条路径实测通过
- [x] 验收标准 4：打包后模型/日志/用户配置全部落 XDG 用户目录，AppImage 挂载点零写入（故障注入验证）
- [~] 验收标准 5：协议级 E2E + 场景脚本通过（CP0 绿）；人工清单待用户逐项 🟢/🔴
- [x] 验收标准 6：资产命名终态核对零残留，迁移清单逐项销账
- [~] 验收标准 7：四项自动化实测通过；GUI 交互人眼复核在人工清单 §6
- [x] `model_ready_timeout_s` 打包形态 P99 回填校准；选型文档状态转「已确认」
- [x] 三组件 README「打包产物使用说明」节与实际行为一致（含离线模型放置）
- [x] 阶段 4 验收记录撰写完成（`文档/阶段4验收记录.md`），提交用户审阅（CP1）

---

*创建于 2026-07-22 14:12 (UTC+8)*
