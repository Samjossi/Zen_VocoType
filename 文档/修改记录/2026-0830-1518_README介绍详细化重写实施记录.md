> ⚠️ **归档声明**：本文档为历史快照，记录了特定时间点的决策与实施状态。
> 当前代码状态以对应源文件为准，本文档内容可能已过时，仅供参考。

# README 介绍详细化重写实施记录

> **状态**：已实施（2026-08-30，T1–T7 全部完成）
> **范围**：`README.md`（整文件重写）、`work plans/2026-0830-1507_README介绍详细化重写计划.md`（状态收尾）
> **时间**：2026-08-30 15:18（UTC+8）
> **依据**：`2026-0830-1507_README介绍详细化重写计划.md`

---

## 一、实施内容

| 任务 | 内容 | 结果 |
|:---|:---|:---|
| T1 | 按计划 §3 大纲整文件重写 `README.md`（102 行 → 约 230 行）：项目简介（定位 + 核心特性 6 条 + 平台与运行形态）、组件总览（四组件详述 + 通信关系）、快速开始（整合原「打包产物使用说明」）、开发态运行与测试、配置体系、仓库结构、文档导航 | ✅ |
| T2 | 信息迁移核对：旧稿 7 个小节（产物布局/构建/桌面入口/开机自启动/数据配置位置/离线模型/故障排查）逐节迁入，事实性内容零丢失；systemd 自启动 🔴 事故警示原样保留 | ✅ |
| T3 | 事实核对：三条 pytest 命令实跑（Service 195 passed / Client 244 passed / Launcher 196 passed + 3 个既有环境依赖失败，见「三、验证结果」）；构建与安装命令与 `tools/` 脚本、`常用命令.md` 一致 | ✅ |
| T4 | 引用核对：文中 29 处文件/目录引用逐一执行存在性检查全部通过；引用格式符合 `文档编写规范.md` §4（反引号纯文本、无行号、无 Markdown 链接） | ✅ |
| T5 | 交叉一致性：组件特性、配置链、托盘行为等表述均直接取自四份组件 README 现行内容，细节注明以组件 README 为准 | ✅ |
| T6 | 本记录 | ✅ |
| T7 | 计划文档状态改「已实施」 | ✅ |

## 二、关键决策落实

- **不加图文**：按用户 2026-08-30 指示收敛，仅保留 ASCII 目录树式代码块，不引入图片/mermaid
- **平台限定写明**：开篇「🔴 仅支持 Linux（不支持 Windows / macOS）」，并注明
  Wayland 会话下 v1 暂不支持全局热键（X11 实现）
- **修正旧引用错误**：旧稿将 systemd 诊断报告引为 `work plans/2026-0723-1705_...`，
  实际位于 `文档/修改记录/`；按规范改为带时间戳文件名引用
  `2026-0723-1705_systemd自启动下启动器托盘失效诊断报告.md`（全局唯一，路径免疫）
- **增量信息**：「数据与配置位置」表补录音/识别文本目录行（源自 Client README T34 节）；
  Launcher 查找顺序补 `~/AppImages` 兜底档（源自 Launcher README「目标解析」节，T40）

## 三、验证结果

```
.venv/bin/python -m pytest Zen_VocoType_Service/tests -q   → 195 passed, 10 deselected
.venv/bin/python -m pytest Zen_VocoType_Client/tests -q    → 244 passed
.venv/bin/python -m pytest Zen_VocoType_Launcher/tests -q  → 196 passed, 3 failed
```

Launcher 的 3 个失败（`test_t39_targets.py::TestProdPlan` 目标解析用例）为
**既有失败**：`git stash` 旧版 README 后复跑同样 3 failed，与本次重写无关
（纯文档改动，且该组用例依赖运行环境的产物布局）。

## 四、偏离说明

- 计划 §3 大纲中「数据与配置位置」表原定第 3、5 章各出现一次，实施时只保留
  第 3 章一处、第 5 章改为指针引用，避免双向维护漂移（与计划 §6「细节漂移」
  风险条目精神一致）。

---

*实施：2026-08-30 15:18 (UTC+8)*
