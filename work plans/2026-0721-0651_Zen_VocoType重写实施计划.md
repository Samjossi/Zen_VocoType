# Zen_VocoType 重写实施计划

> **状态**：草稿
> **范围**：Zen_VocoType 全组件重写（阶段 0 ~ 阶段 4）
> **时间**：2026-07-21 06:51（设计）
> **优先级**：高

---

## 1. 前置文档

| 文档 | 路径 | 状态 |
|:---|:---|:---|
| 重写大纲 | [`work charter/2026-0721-0529_GridChat迁移Zen_VocoType重写大纲.md`](../work%20charter/2026-0721-0529_GridChat迁移Zen_VocoType重写大纲.md) | 草稿，待审阅确认 |
| 技术选型 | [`work options/2026-0721-0557_阶段0技术选型对比.md`](../work%20options/2026-0721-0557_阶段0技术选型对比.md) | 草稿，待审阅确认 |

> 本计划落地以上两份文档。阶段 0 的交付物即为后续阶段的前提；选型结论已内嵌到各阶段任务中。

---

## 2. 阶段总览

| 阶段 | 名称 | 核心交付物 | 依赖 |
|:---:|:---|:---|:---|
| 0 | 设计定稿 | 协议设计文档、协议契约库、目录结构、资产迁移 | 大纲 + 选型审阅通过 |
| 1 | Service 实现 | Socket 服务、模型管理、协议层、PID 管理 | 阶段 0 |
| 2 | Client 实现 | 热键、录音、输出、托盘、状态机 | 阶段 0 + 阶段 1（联调） |
| 3 | Launcher 实现 | 编排、就绪等待、锁、清理、dev 模式 | 阶段 1 + 阶段 2 |
| 4 | 打包与端到端 | 三组件打包产物、端到端验收报告 | 阶段 1 + 2 + 3 |

---

## 3. 阶段 0 — 设计定稿

### 3.1 任务清单

| # | 任务 | 优先级 | 预计耗时 | 产出 |
|:---:|:---|:---:|:---|:---|
| 0-1 | 制定通信协议设计文档：帧格式（复合帧：4 字节头长度 + JSON 头 + 二进制音频体）、action 全集（`health`/`ready`/`recognize`/`model_info`/`model_switch`）、错误码、版本握手机制 | 高 | 2h | [`文档/`](../文档/) 下协议设计文档 |
| 0-2 | 搭建协议契约库 `Zen_VocoType_Protocol/`：帧编解码、MessageBuffer、action 常量、错误码枚举、版本号、`pyproject.toml`；以 `pip install -e` 装入 `.venv` | 高 | 2h | 可 import 的协议包 |
| 0-3 | 制定三组件目录结构：每组件独立 `pyproject.toml`、入口 `main.py`、`src/` 子模块划分（参考大纲 §3.1 职责 + §5-10 模块拆分约束）；根目录共享 `.venv` + 协议包 editable install | 高 | 1h | 三组件骨架目录 + README 更新 |
| 0-4 | 资产迁移：复制旧图标 → 重命名为 `zen_vocotype_*` → 放入各组件 `assets/`；图标加载 helper（自定位路径，兼容 `_MEIPASS`） | 中 | 0.5h | 三组件 `assets/` 目录 |
| 0-5 | 配置方案落地：每组件 pydantic-settings `Settings` 类 + JSON 配置文件；Socket 路径默认值写入协议契约库 | 中 | 1h | 三组件 `config.py` 骨架 |
| 0-6 | UI 最小验证（选型 §11-3）：空 PySide6 托盘程序 → PyInstaller onedir 打包 → 运行验证托盘图标可见；不通过则回退评估 GTK | 高 | 2h | 验证通过/不通过的结论记录 |

### 3.2 检查点

- [ ] 0-1~0-3 完成：`python -c "from zen_vocotype_protocol import ..."` 成功；三组件 `python main.py --help` 正常输出
- [ ] 0-4 完成：图标文件存在且命名无旧残留
- [ ] 0-5 完成：三组件 Settings 可加载，Socket 路径默认值从协议包读取
- [ ] 0-6 完成：打包后托盘可见 → PySide6 方案确认；否则记录回退

### 3.3 依赖

- 大纲文档审阅通过（状态由「草稿」→「已确认」）
- 选型文档审阅通过（状态由「草稿」→「已确认」）

---

## 4. 阶段 1 — Zen_VocoType_Service

### 4.1 任务清单

| # | 任务 | 优先级 | 预计耗时 | 产出 |
|:---:|:---|:---:|:---|:---|
| 1-1 | Socket 服务层：`listen()` + `accept()` + 消息路由分发（基于协议契约库的 action 枚举）；单客户端连接处理 | 高 | 3h | `src/socket_server.py` |
| 1-2 | 模型注册表：`ModelRegistry` 数据类，注册 paraformer-large / sensevoice / paraformer-streaming 三种模型配置（name、model_id、采样率、语言） | 高 | 1h | `src/model_registry.py` |
| 1-3 | 模型加载服务：按 model_name 分发加载逻辑（🔴 禁止硬编码单模型）；`run_in_executor` 异步加载；加载失败显式报错（🔴 禁止假模型兜底） | 高 | 4h | `src/speech_service.py` |
| 1-4 | 先监听后加载：启动流程 = 绑定 Socket + `listen()` → 异步加载模型 → 模型就绪后 `ready` 响应返回 `True`；冷启动 Socket 可连接目标 ≤ 5 秒 | 高 | 2h | `main.py` 启动流程 |
| 1-5 | 模型下载/校验：`ModelManager`（下载、磁盘校验、可用空间检查），`model_switch` action 触发下载 + 切换，切换后 `model_info` 可验证 | 中 | 3h | `src/model_manager.py` |
| 1-6 | `health` / `ready` / `recognize` / `model_info` / `model_switch` 全 action 实现，响应格式遵循协议设计文档 | 高 | 3h | 各 action handler |
| 1-7 | PID 文件管理：启动时写 PID、退出时清理；优雅停服信号处理（SIGTERM/SIGINT → 停止接受新连接 → 完成当前请求 → 退出） | 中 | 1h | `src/process_manager.py` |
| 1-8 | Logging：loguru 配置，控制台 + 滚动文件 + error.log；🔴 禁止 print 当日志 | 中 | 0.5h | `src/logging_config.py` |

### 4.2 检查点

- [ ] 1-1 + 1-4：`python main.py` 启动后 5 秒内 Socket 可连接
- [ ] 1-2~1-6：客户端脚本（`python -c` 快速测试）依次发送 `health`→`ready`→`recognize`→`model_info`→`model_switch`，全部正确响应
- [ ] 1-7：PID 文件写入与清理正常；SIGTERM 优雅退出
- [ ] 1-5 + 1-3：模型切换真实生效，`model_info` 交叉验证通过；模型不存在时报错而非静默

### 4.3 依赖

- 阶段 0 全部完成
- FunASR 已安装至 `.venv`

---

## 5. 阶段 2 — Zen_VocoType_Client

### 5.1 任务清单

| # | 任务 | 优先级 | 预计耗时 | 产出 |
|:---:|:---|:---:|:---|:---|
| 2-1 | 录音模块（`AudioRecorder`）：sounddevice 16kHz/单声道/16bit；实例生命周期复用（🔴 禁止每次新建）；录制回调产生 PCM 帧 | 高 | 2h | `src/audio_recorder.py` |
| 2-2 | 热键模块（`HotkeyBackend` 抽象 + `PynputBackend` 实现）：按住开始、松开停止；`backend` 抽象供 Wayland 迁移 | 高 | 2h | `src/hotkey/` |
| 2-3 | Socket 客户端（`SpeechSocketClient`）：基于协议契约库编解码；长连接复用（🔴 禁止每次新建连接）；连接失败明确报错 + 自动重连 | 高 | 3h | `src/socket_client.py` |
| 2-4 | 文字输出模块：剪贴板备份 → 写入 → Ctrl+V → 延迟恢复（命名常量 + 可配置）；恢复前校验剪贴板内容；降级 xdotool type；降级时 warning + 通知 | 高 | 3h | `src/text_output.py` |
| 2-5 | 状态机（`ClientStateMachine`）：`idle → recording → transcribing → completed/error → idle`；状态变更驱动 UI 更新 | 高 | 1h | `src/state_machine.py` |
| 2-6 | 托盘 UI（PySide6 `QSystemTrayIcon`）：状态显示 / 开始停止录音 / 设置 / 退出；无托盘降级为通知模式 | 高 | 3h | `src/tray/` |
| 2-7 | 事务编排（`AppManager`）：热键→录音→识别→输出的完整链路编排；热键松开→录音停止→发送识别→收到结果→文字输出 | 高 | 2h | `src/app_manager.py` |
| 2-8 | 单一配置（pydantic-settings）：录音参数、热键绑定、Socket 路径（从协议包默认值覆盖）、输出恢复延迟 | 中 | 1h | `src/config.py` |
| 2-9 | Logging：与 Service 统一风格 | 中 | 0.5h | `src/logging_config.py` |
| 2-10 | Service 联调：按住热键 → 录音 → 识别 → 文字出现全流程可用；Service 未启动时明确提示 | 高 | 2h | 联调通过 |

### 5.2 检查点

- [ ] 2-1：录音 5 秒 → WAV 文件可播放
- [ ] 2-2：热键按住/松开触发正确
- [ ] 2-3：连接 Service 发送 `health`，正确响应
- [ ] 2-4：写入文本 → Ctrl+V → 文字出现在编辑器 → 剪贴板恢复为原内容
- [ ] 2-5~2-7：完整链路：按住热键 → 说话 → 松开 → 文字出现在光标处
- [ ] 2-6：托盘图标显示、菜单可操作、拔掉托盘后降级为通知
- [ ] 2-10：Service 未运行时启动 Client → 提示「服务端未运行」→ 不崩溃

### 5.3 依赖

- 阶段 0 完成
- 阶段 1 完成（联调）
- sounddevice / pynput / PySide6 已安装至 `.venv`

---

## 6. 阶段 3 — Zen_VocoType_Launcher

### 6.1 任务清单

| # | 任务 | 优先级 | 预计耗时 | 产出 |
|:---:|:---|:---:|:---|:---|
| 3-1 | 单实例锁：文件锁（`fcntl.flock`）防 Launcher 自身重复启动；锁定期间另一实例探测到锁 → 检查已有进程 → 幂等退出 | 高 | 1h | `src/instance_lock.py` |
| 3-2 | 进程检测：可执行路径精确匹配（psutil `cmdline` 精确比对，🔴 禁止子串匹配）；区分「进程在运行」与「进程不存在」 | 高 | 1h | `src/process_checker.py` |
| 3-3 | 服务端拉起 + 就绪等待：Popen 拉起 → 协议级 `ready` 探测（Socket connect → 发 `ready` → 等 `True`）；超时预算基于 P99 冷启动实测值（初始 180s，实测后调整）；区分「进程已死」与「仍在启动」 | 高 | 3h | `src/server_launcher.py` |
| 3-4 | 客户端拉起：`ready` 通过后拉起；启动失败明确报错 + 通知 | 中 | 1h | `src/client_launcher.py` |
| 3-5 | 失败清理：任一步骤失败 → kill 已拉起的子进程（PID 追踪）→ 删除临时锁文件 → 发错误通知 → 非 0 exit code | 高 | 1h | `src/cleanup.py` |
| 3-6 | `--dev` 模式：不拉二进制，改用 `.venv` 的 `python main.py` 拉起两端；Socket 路径自动切换为 dev 路径（`/tmp/zen_vocotype_dev.sock`） | 中 | 1h | `main.py` 参数分支 |
| 3-7 | 状态通知（notify-send）：启动中 / 全部完成 / 失败 / 重复启动幂等退出；🔴 失败后不再发「全部完成」通知（旧项目反面案例） | 中 | 0.5h | `src/notifier.py` |
| 3-8 | CLI 入口与 exit code：`main.py` 参数解析；run() 返回值正确映射为 exit code（🔴 禁止旧版恒 0） | 中 | 0.5h | `main.py` |

### 6.2 检查点

- [ ] 3-1：连续运行两次 Launcher → 第二次「幂等退出」通知 + exit 0
- [ ] 3-2 + 3-3：Service 已在运行 → Launcher 跳过拉起、等待 `ready` → 成功
- [ ] 3-3：模拟 Service 慢启动（sleep 注入）→ Launcher 正确等待 → 不误判
- [ ] 3-3：模拟 Service 启动后立即崩溃 → Launcher 正确报错 + exit 1 + 清理
- [ ] 3-6：`--dev` 模式拉起两端源码 + dev Socket → 功能与正式模式一致
- [ ] 3-5：Service 拉起后 Launcher 超时退出 → Service 被 kill，无孤儿进程

### 6.3 依赖

- 阶段 1 + 阶段 2 完成
- psutil 已安装至 `.venv`

---

## 7. 阶段 4 — 打包与端到端

### 7.1 任务清单

| # | 任务 | 优先级 | 预计耗时 | 产出 |
|:---:|:---|:---:|:---|:---|
| 4-1 | 打包模板体系：三组件统一 PyInstaller spec 模板（hidden imports、datas、品牌图标收编）；根目录 `build.py` 一键构建三组件 | 高 | 3h | `build.py` + 三组件 `*.spec` |
| 4-2 | Service 打包：onedir + 图标 `assets/` 收编；打包后实测 Socket 可连接时间 + 模型异步加载 | 高 | 1h | Service 打包产物 |
| 4-3 | Client 打包：onedir + 图标收编 + Qt 插件收编（QPA/平台插件）；打包后实测托盘 + 热键 + 全流程 | 高 | 2h | Client 打包产物 |
| 4-4 | Launcher 打包：onedir + 图标收编；`--dev` 模式下可指向源码目录（打包产物中保留 dev 路径配置） | 中 | 1h | Launcher 打包产物 |
| 4-5 | AppImage 封装：三组件 onedir → AppImage（squashfs）；`grid_chat.desktop` 重写为 Zen_VocoType 版 | 中 | 2h | 三个 `.AppImage` 产物 |
| 4-6 | 端到端验收：打包产物部署到干净环境（或容器）→ 启动全流程一次成功 → 记录启动耗时 → 与旧版 66 秒对比 | 高 | 3h | 端到端验收报告 |
| 4-7 | 图标实测：打包后托盘图标、通知图标、.desktop 图标全部可见 | 中 | 0.5h | 验收记录 |

### 7.2 检查点

- [ ] 4-1：`python build.py --all` 产出三组件 onedir 目录
- [ ] 4-6：干净环境全流程「按住热键 → 说话 → 松开 → 文字出现」一次成功
- [ ] 4-6：总冷启动耗时 < 30 秒（对比旧版 66 秒）
- [ ] 4-7：所有图标在打包环境下正常显示

### 7.3 依赖

- 阶段 1 + 2 + 3 全部完成
- PyInstaller 已安装至 `.venv`
- AppImage 工具（`appimagetool`）可用

---

## 8. 全局依赖与风险

| 风险 | 影响 | 应对 |
|:---|:---|:---|
| PySide6 + PyInstaller 打包后 Qt 插件缺失 | 托盘/主题失效 | 阶段 0 最小验证（0-6）提前暴露；阶段 4 early test |
| FunASR 模型路径在打包后解析失败 | 冷启动慢或报错 | 🔴 路径自定位（C3）+ 每次打包实测 |
| 某桌面环境 AppIndicator/SNI 不支持托盘 | 用户无托盘 | 无托盘降级模式已纳入计划 |
| pynput 在 Wayland 下失效 | 热键不可用 | v1 锁定 X11；`HotkeyBackend` 抽象留迁移路径 |

---

## 9. 预计总耗时

| 阶段 | 预计耗时 |
|:---:|:---|
| 0 — 设计定稿 | ~8.5h |
| 1 — Service | ~16.5h |
| 2 — Client | ~17h |
| 3 — Launcher | ~8.5h |
| 4 — 打包与端到端 | ~12.5h |
| **合计** | **~63h** |

> 预计耗时为粗估，实际以验收通过为准，不追求速度。

---

*创建于 2026-07-21 06:51 (UTC+8)*
