# README 介绍详细化重写计划

> **状态**：已实施（2026-08-30，T1–T7 全部完成；实施记录见 `2026-0830-1518_README介绍详细化重写实施记录.md`）
> **范围**：`README.md`（仅根目录项目总览，单文件重写）
> **时间**：2026-08-30 15:07 (UTC+8)
> **优先级**：低

---

## 1. 目标

将根目录 `README.md` 的顶部介绍从目前的「一句话 + 四组件名列表」扩写为**详细的项目介绍**，让初次接触仓库的人只看这一份文档就能理解：项目是什么、怎么用、由什么组成、去哪里查细节。

### 1.1 边界（不做什么）

- 🔴 **不加图文**：不引入图片、mermaid、截图；仅保留现有 ASCII 目录树式代码块（用户 2026-08-30 明确收敛）
- 不动各组件 README（`Zen_VocoType_Service/README.md` 等四份），细节仍由它们承载，根 README 只做导航不重复细节
- 不改任何代码、配置、测试
- 现有「打包产物使用说明」全部信息**只整合不删减**（可调整归属章节与措辞，事实性内容不得丢失）

---

## 2. 现状分析

现有 `README.md` 共 102 行，两部分：

1. **介绍段（第 1–10 行）**：一句话定位 + 四组件各一句话职责 + 一句命令索引。问题：没有特性说明、没有使用场景、没有形态区分（开发态/打包态）、没有文档导航，新读者必须点进组件 README 才能拼出全貌。
2. **打包产物使用说明（第 14–102 行）**：产物布局、构建、桌面入口、开机自启动、数据配置位置、离线模型、故障排查。内容完整，直接并入新结构即可。

重写属于纯文档改写，无技术风险，重点是**结构重组 + 介绍扩写 + 信息无损迁移**。

---

## 3. 新 README 结构大纲

```
# Zen_VocoType

1. 项目简介
   - 一段话定位：Linux 桌面本地离线语音输入，按住热键说话、松开识别并粘贴到光标处
   - 核心特性列表（5–8 条）：
     · 纯本地离线识别（FunASR / ModelScope，GGUF 量化模型，约 1.2GB，首次自动下载）
     · 按住说话 / 松开上屏的即按即说交互
     · 三组件系统托盘（状态色、切引擎、改快捷键、设置模型目录）
     · Launcher 一键拉起全套：幂等、崩溃回收、成功后自退
     · XDG 分层数据布局，AppImage 只读挂载零写入
     · 纯用户态安装（桌面菜单 + 开机自启动，无需 root / systemd）
   - 平台与形态：仅 Linux；AppImage 单文件 / onedir 裸目录 / 源码开发态三种运行形态

2. 组件总览
   - 四组件各一小段（比现在的一句话更详细：职责 + 关键技术点，3–4 行/个），
     并注明各自细节见组件 README
   - 通信关系文字简述：Client/Launcher 经 Unix Socket 与 Service 通信，
     帧格式与错误码唯一出处为 Zen_VocoType_Protocol 契约库

3. 快速开始（打包产物）
   - 整合现有「打包产物使用说明」全部小节：
     产物布局 → 构建 → 桌面入口安装/卸载 → 开机自启动 → 数据与配置位置 →
     离线模型手工放置 → 故障排查
   - 事实性内容逐条对照原稿核对，确保零丢失（含 systemd 自启动 🔴 警示）

4. 开发态运行与测试
   - 最小命令示例（.venv 运行三组件、pytest 入口）
   - 详细命令索引指向 `常用命令.md`

5. 配置体系
   - 配置链一段：组件默认值 → 包内 config.yaml → 用户配置 → 环境变量
     （前缀 ZEN_VOCOTYPE_<组件>_）
   - 数据/日志/模型/Socket 位置表（沿用现有表格）

6. 仓库结构
   - 顶层目录树（四组件 + tools/ + 文档/ + work plans/ 等，一行注释/项）

7. 文档导航
   - 各组件 README（4 份）
   - `常用命令.md`、`文档编写规范.md`、`AGENTS.md`
   - `文档/` 八个子目录的用途一句话/个
   - 文件引用一律反引号纯文本，不做 Markdown 链接（遵循 `文档编写规范.md` §4）
```

---

## 4. 任务清单

### 阶段一：起草

- [ ] **T1** 按 §3 大纲整文件重写 `README.md`（Write 全量替换；旧文件 102 行已通读，无其他依赖方引用其行号）
- [ ] **T2** 信息迁移核对：将现有「打包产物使用说明」7 个小节逐节映射到新结构第 3/5 章，列出迁移对照表确认无遗漏

### 阶段二：校对

- [ ] **T3** 事实核对：文中每条命令（`tools/build.py`、`tools/install_desktop.py` 等）、每个路径（XDG 各目录、模型缓存布局）、每个配置键与现状一致
- [ ] **T4** 引用核对：所有提到的文件/目录在仓库中真实存在；引用格式符合 `文档编写规范.md` §4（反引号纯文本、无行号）
- [ ] **T5** 交叉一致性：与四份组件 README 的同名主题（托盘、配置、打包、测试）措辞不矛盾，细节以组件 README 为准并在文中注明

### 阶段三：收尾

- [ ] **T6** 修改记录：按 `文档/修改记录/` 既有格式补一条重写记录
- [ ] **T7** 本计划状态改「已实施」

---

## 5. 验证方法

| 检查 | 方法 | 预期 |
|:---|:---|:---|
| 信息无损 | 新稿与旧稿逐节对照 | 旧稿全部事实性内容可在新稿找到对应 |
| 路径真实 | 对文中每个引用路径执行存在性检查 | 全部存在 |
| 命令正确 | 与 `常用命令.md`、`tools/` 脚本实际入参比对 | 一致 |
| 规范符合 | 对照 `文档编写规范.md` §4 | 反引号引用、无行号、无 Markdown 链接 |
| 渲染正常 | Markdown 预览 | 标题层级、表格、代码块无破损 |

---

## 6. 风险与注意

- **细节漂移**：根 README 只写「是什么 + 怎么用 + 去哪查」，不写组件内部实现细节，避免日后与组件 README 双向维护漂移
- **篇幅控制**：目标 150–200 行（现为 102 行），详细但不冗长；打包产物章节占大头属预期
- **🟡 自启动警示保留**：systemd 自启动 🔴 事故警示（`2026-0723-1705_systemd自启动下启动器托盘失效诊断报告.md`）必须原样保留在醒目位置，不得因重排弱化

---

*撰写：2026-08-30 15:07 (UTC+8) | 待审阅后实施*
