# Git 历史隐私清洗实施计划

> **状态**：已实施（2026-08-30 当日完成，本文档为实施后补写存档）
> **范围**：全仓库 Git 历史（master 分支，80 个提交）+ 本地 git 配置 + Forgejo/GitHub 双远端
> **时间**：2026-08-30 14:35 (UTC+8)（设计）/ 2026-08-30 15:10（实施完成）
> **依据**：`2026-0830-1430_Git历史隐私泄露审计报告.md`

---

## 1. 背景与目标

审计（见依据文档）确认：仓库历史无密钥/录音/日志等硬泄露，但 79 个提交的作者字段含真实 Outlook 邮箱、全历史约 560 处用户家目录绝对路径（`/home/<用户名>/...`），二者可串联定位到个人。历史当时仅存在于私有 Forgejo 与本地，**从未进入公开平台**——这是「先清洗、再公开」路径成立的唯一窗口期。

目标：在首次推送 GitHub 前完成历史重写，使公开平台从第一天起只见到干净历史，不留任何旧历史记录。

## 2. 决策记录（用户裁决，2026-08-30）

| 决策项 | 结论 |
|:---|:---|
| 公开作者身份 | `Samjossi <144557423+Samjossi@users.noreply.github.com>`（GitHub noreply，用户 ID 经 GitHub API 核实） |
| 路径与署名替换 | 家目录绝对路径 → `~`；旧署名英文 ID → `Samjossi` |
| vendor 二进制 | 保留 `Zen_VocoType_Service/bin/llama-funasr-cli` 5.9MB（合法 vendor，剔除破坏历史自包含性） |
| Forgejo 旧历史 | 强推清洗版覆盖，双端历史一致；清洗前本地镜像备份在推送验证无误后删除 |
| 笔名「afro zen」 | 接受保留，不纳入替换范围 |

## 3. 实施步骤（已执行）

1. **备份**：`git clone --mirror` 至 `.temp/git-backup-20260830-1435.git`（80 提交；已于推送验证后删除）
2. **前置提交**：`2026-0803-0226_MoonBit组件级重写参赛计划.md` 状态变更（待审阅 → 暂时不做）先行入库
3. **环境准备**：`uv pip install git-filter-repo`（2.47.0）至项目 `.venv`；工作树保持干净（未跟踪文件不受影响）
4. **历史重写**：`git filter-repo --force --mailmap <身份映射> --replace-text <替换表>`，80 个提交一次跑完
5. **验证**：全历史 `git grep` 残留扫描（原路径/原邮箱/`/home/` 均 0 命中）；四组件测试回归；3 项 Launcher 失败经备份仓库复跑对照
6. **收尾提交**：审计报告更新为「已实施」并自身脱敏后入库（`00fc7b1`）；本地 `git config` 切换 noreply 身份防增量泄露
7. **推送**：GitHub 首推（`master` 新分支）+ Forgejo `--force` 强推（`8de7769...00fc7b1` forced update）
8. **删除备份**：用户确认 GitHub 端无误后删除镜像备份，清洗前历史彻底不再留存

## 4. 验证结果

| 验证项 | 结果 |
|:---|:---|
| 残留扫描 | 原用户名路径 / 原邮箱 / `/home/` 全历史 0 命中；commit message 经核查本不含敏感串 |
| 作者身份 | `git log` 全历史仅见 noreply 身份；GitHub API 抽查提交确认邮箱为 noreply |
| 测试回归 | Protocol 63 / Client 239 / Service 195 / Launcher+tools 204 全绿 |
| 已知例外 | Launcher `test_t39_targets.py` 3 项失败，经清洗前备份复跑证实为**环境性既有问题**（本机 `~/AppImages/` 存在真实 AppImage 触发兜底目录解析），与清洗无关，可单独立项修复测试隔离 |
| 双端一致性 | GitHub 与 Forgejo 均指向 `00fc7b1` |

## 5. 遗留事项

- 历史重写后所有 commit hash 变更，`文档/修改记录/` 等文档中引用的旧 hash 失效（已接受的副作用）
- 笔名「afro zen」保留于 `文档/提示语句/约束随机搜索方法论_在混沌解空间中寻路_v1.0.md`（用户裁决保留）
- 建议 GitHub 设置 → Emails 勾选 "Keep my email addresses private" 与 "Block command line pushes that expose my email" 双保险
- 后续若新增公开远端，一律推送清洗后历史，禁止从任何旧克隆恢复推送

---

*补写：2026-08-30 15:20 (UTC+8) | 实施与验证记录详见 `2026-0830-1430_Git历史隐私泄露审计报告.md` §6*
