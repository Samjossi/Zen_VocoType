[project]
name = "zen-vocotype"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
markers = [
    "slow: 慢速测试（真实模型加载/端到端），默认一并运行，可用 -m 'not slow' 跳过",
]
