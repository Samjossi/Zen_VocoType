# 常用命令

> Zen_VocoType 日常运行命令速查（全部在项目根目录执行，统一使用 `uv run`，自动使用项目 `.venv`）

---

## 启动运行

```bash
# 启动服务端（先启动；托盘橙色→绿色约需十几秒模型加载）
uv run python Zen_VocoType_Service/main.py

# 启动客户端（另开一个终端；托盘出现麦克风图标）
uv run python Zen_VocoType_Client/main.py

# 启动器（默认托盘模式：拉起服务端与客户端，成功后自行退出；
# 无显示环境自动回退一次性 CLI；加 --no-tray 强制一次性 CLI）
uv run python Zen_VocoType_Launcher/main.py
```

正常使用时：托盘图标变绿后，**按住 `<ctrl>+<alt>+y` 说话，松开自动识别并粘贴**。

```bash
# 停止：两端均可 Ctrl+C，或托盘菜单「退出」
```

## 配置

| 文件 | 用途 |
|:---|:---|
| `Zen_VocoType_Client/config.yaml` | 客户端唯一配置源（热键、录音上限、恢复延迟等） |
| `Zen_VocoType_Service/config.yaml` | 服务端唯一配置源（模型注册表等） |
| `Zen_VocoType_Launcher/config.yaml` | 启动器唯一配置源（启动延迟、启动间隔、组件位置等） |

热键修改示例（改完重启客户端生效）：

```yaml
# Zen_VocoType_Client/config.yaml
hotkey: "<ctrl>+<alt>+y"
```

---

*创建于 2026-07-21 17:06 (UTC+8)；更新于 2026-07-23 11:22 (UTC+8)：精简为仅运行命令（T40 启动器托盘模式），打包/测试/截图自查命令移出*
