# 常用命令

> Zen_VocoType 日常运行与测试命令速查（全部在项目根目录执行，统一使用 `uv run`，自动使用项目 `.venv`）

---

## 1. 启动运行

```bash
# 启动服务端（先启动；托盘橙色→绿色约需十几秒模型加载）
uv run python Zen_VocoType_Service/main.py

# 启动客户端（另开一个终端；托盘出现麦克风图标）
uv run python Zen_VocoType_Client/main.py
```

正常使用时：托盘图标变绿后，**按住 `<ctrl>+<alt>+o` 说话，松开自动识别并粘贴**。

```bash
# 停止：两端均可 Ctrl+C，或托盘菜单「退出」（客户端）
```

## 2. 测试

```bash
# 客户端全部测试（110 项，含真实服务端 E2E，约 25 秒）
uv run pytest Zen_VocoType_Client/tests/ -q

# 客户端快速测试（跳过慢速 E2E）
uv run pytest Zen_VocoType_Client/tests/ -q -m "not slow"

# 服务端全部测试
uv run pytest Zen_VocoType_Service/tests/ -q

# 契约库测试
uv run pytest Zen_VocoType_Protocol/tests/ -q
```

## 3. 托盘布局截图自查

```bash
# 托盘图标/右键菜单/状态色截图（产物在指定目录，含菜单特写与状态色全览）
uv run python Zen_VocoType_Client/main.py --screenshot .temp/截图自查
```

## 4. 打包验证

```bash
# 打包客户端（onedir，产物在 .temp/打包/dist/；uv 会自动向上找到项目根）
mkdir -p .temp/打包 && cd .temp/打包 && \
  uv run pyinstaller --onedir --windowed --noconfirm --clean \
  --name zen_vocotype_client \
  --add-data "$PWD/../../Zen_VocoType_Client/assets:assets" \
  ../../Zen_VocoType_Client/main.py && cd ../..

# 运行打包产物
.temp/打包/dist/zen_vocotype_client/zen_vocotype_client

# 打包产物截图自查
.temp/打包/dist/zen_vocotype_client/zen_vocotype_client --screenshot .temp/打包截图
```

## 5. 日志查看

```bash
# 客户端日志
tail -f Zen_VocoType_Client/logs/client.log

# 服务端日志
tail -f Zen_VocoType_Service/logs/service.log
```

## 6. 配置

| 文件 | 用途 |
|:---|:---|
| `Zen_VocoType_Client/config.yaml` | 客户端唯一配置源（热键、录音上限、恢复延迟等） |
| `Zen_VocoType_Service/config.yaml` | 服务端唯一配置源（模型注册表等） |

热键修改示例（改完重启客户端生效）：

```yaml
# Zen_VocoType_Client/config.yaml
hotkey: "<ctrl>+<alt>+o"
```

---

*创建于 2026-07-21 17:06 (UTC+8)；更新于 2026-07-22 12:46 (UTC+8)：命令统一改为 `uv run`*
