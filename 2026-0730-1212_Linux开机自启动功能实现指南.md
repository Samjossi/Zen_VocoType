# Linux 开机自启动功能实现指南（XDG Autostart）

> **状态**：已实施
> **范围**：`src/infrastructure/autostart_manager.py`、`main.py`、`src/presentation/settings_dialog.py`
> **时间**：2026-07-30 12:12 (UTC+8)（设计）/ 2026-07-30 12:12（实施）
> **优先级**：中

---

## 1. 功能概述

设置对话框中的「系统启动时自动运行」勾选框，控制应用在用户登录桌面环境后是否自动启动。

本实现遵循 **XDG Autostart 规范**（freedesktop.org 标准），被 GNOME、KDE Plasma、XFCE 等主流 Linux 桌面环境支持。核心机制是：在 `~/.config/autostart/` 目录下维护一个 `.desktop` 文件，桌面环境在登录时会扫描该目录并执行其中每个文件的 `Exec` 命令。

> ✅ 该机制是**纯文件操作**，无需 root 权限、无需 systemd 服务、无需与桌面环境 IPC 通信，是 Linux 桌面应用实现自启动的标准做法。

---

## 2. 核心原理：一个文件控制一切

```
~/.config/autostart/zen-copyq.desktop
```

| 文件状态 | 效果 |
|:---|:---|
| 文件不存在 | 不自启动 |
| 文件存在且 `Hidden=true` | 不自启动（规范定义的"禁用"语义） |
| 文件存在且 `Hidden=false` 或无 `Hidden` 行 | 自启动，登录时执行 `Exec=` 行 |

示例 desktop 文件内容：

```ini
[Desktop Entry]
Name=Zen CopyQ
Icon=zen-copyq
GenericName=Clipboard Manager
Type=Application
Terminal=false
X-KDE-autostart-after=panel
X-KDE-StartupNotify=false
X-KDE-UniqueApplet=true
X-GNOME-Autostart-Delay=3
Exec="/path/to/python" "/path/to/main.py"
Hidden=false
X-GNOME-Autostart-enabled=true
```

关键字段说明：

| 字段 | 作用 |
|:---|:---|
| `Exec` | 登录时执行的命令（每次保存时重写，保证路径最新） |
| `Hidden` | XDG 规范定义的禁用开关，`true` 表示禁用 |
| `X-GNOME-Autostart-enabled` | GNOME 旧版兼容字段，与 `Hidden` 保持反向同步 |
| `X-KDE-autostart-after=panel` | KDE 下等面板加载完再启动（托盘类应用需要） |
| `X-GNOME-Autostart-Delay` | GNOME 下延迟启动秒数，减轻登录瞬间负载 |

---

## 3. 代码结构

实现集中在 `src/infrastructure/autostart_manager.py` 的 `AutostartManager` 类，对外暴露 4 个方法：

| 方法 | 职责 |
|:---|:---|
| `is_supported()` | 平台判断（当前仅 Linux） |
| `is_enabled()` | 读取 desktop 文件，解析 `Hidden` 行得出当前状态 |
| `set_enabled(enable)` | 原子写入/更新 desktop 文件 |
| `remove_desktop_file()` | 彻底删除文件（卸载场景） |

### 3.1 状态读取的三态逻辑

`is_enabled()` 不能简单地判断"文件是否存在"，必须解析 `Hidden` 字段，因为规范允许"文件存在但被禁用"的状态：

- 文件不存在 → 未启用
- 存在且 `Hidden=true` → 未启用
- 存在且 `Hidden=false` 或无 `Hidden` 行 → 已启用

### 3.2 原子写入

写入采用"临时文件 + rename"策略，避免桌面环境在登录瞬间读到写了一半的文件：

```python
# src/infrastructure/autostart_manager.py
new_path = self.desktop_path.with_suffix(".desktop.new")
new_path.write_text("".join(lines_to_write), encoding="utf-8")
# 原子替换：POSIX rename 会覆盖同名文件
new_path.replace(self.desktop_path)
```

### 3.3 保留用户已有配置

`set_enabled()` 不是每次生成全新文件，而是读取已有文件、**只过滤替换三个字段**（`Hidden`、`X-GNOME-Autostart-enabled`、`Exec`），其余字段（如用户手动添加的延迟、环境变量）原样保留。文件不存在时才使用内置模板。

### 3.4 Exec 命令的多模式适配

`_get_exec_cmd()` 按优先级生成启动命令：

1. **AppImage 模式**：检测到 `$APPIMAGE` 环境变量时，直接以 AppImage 文件路径作为命令（AppImage 本身可执行）。
2. **开发/直接运行模式**：`"python解释器" "main.py路径"`，路径取自 `sys.executable` 和项目根目录的 `main.py`。
3. **兜底**：仅用当前 Python 解释器路径。

> ⚠️ 所有路径都加双引号，防止路径含空格导致 desktop 文件解析失败。

---

## 4. 双端同步设计

自启动状态存在**两个事实来源**：应用内配置项（`general.autostart`）和系统级 desktop 文件。两者可能不一致（例如用户手动删除了 desktop 文件），因此有两个同步点：

### 4.1 应用启动时同步（配置 → 系统）

`main.py` 启动早期执行一致性检查，以配置项为准：

```python
# main.py
if AutostartManager.is_supported():
    autostart_mgr = AutostartManager()
    is_autostart_configured = bool(set_config.get(ConfigKey.AUTOSTART, False))
    is_autostart_enabled = autostart_mgr.is_enabled()
    if is_autostart_configured != is_autostart_enabled:
        autostart_mgr.set_enabled(is_autostart_configured)
```

### 4.2 设置保存时同步（UI → 配置 → 系统）

`src/presentation/settings_dialog.py` 中：

1. 勾选框状态绑定配置键 `ConfigKey.AUTOSTART`（值为 `general.autostart`）；
2. 平台不支持时禁用勾选框并提示；
3. 用户点击保存后，先将控件值写回配置存储，再调用 `set_enabled()` 落到系统层。

### 4.3 同步时序图

```
用户勾选/取消勾选
      │
      ▼
设置对话框保存 ──写──▶ 配置文件 (set_config.toml)
      │
      ▼
AutostartManager.set_enabled() ──原子写──▶ ~/.config/autostart/*.desktop
      │
      ▼
下次应用启动时 main.py 校验两端一致，不一致则以配置为准修复
```

---

## 5. 其他工程师移植清单

在其他项目中复用这套机制，需要以下内容：

| 步骤 | 内容 |
|:---|:---|
| 1 | 确定应用标识：`app_name`（desktop 文件名，小写连字符风格）与显示名 |
| 2 | 实现状态读取：解析 `~/.config/autostart/<app_name>.desktop` 的 `Hidden` 字段，处理三态 |
| 3 | 实现状态写入：模板 + 字段过滤替换 + 临时文件原子 rename |
| 4 | 实现 Exec 命令生成：按分发模式（打包/开发）适配，路径加引号 |
| 5 | 增加配置项持久化（如 `general.autostart`），与系统状态双端同步 |
| 6 | UI 层：勾选框绑定配置键，平台不支持时禁用控件 |
| 7 | 启动时做一次一致性校验（配置为准） |

---

## 6. 跨平台扩展参考

本实现仅覆盖 Linux。其他平台的对应机制：

| 平台 | 机制 | 说明 |
|:---|:---|:---|
| Linux | `~/.config/autostart/*.desktop` | 本文档所述方案，XDG 标准 |
| Windows | 注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` | 写入应用路径字符串值即可 |
| macOS | `~/Library/LaunchAgents/*.plist` 或 SMAppService API | LaunchAgent plist 定义 `RunAtLoad=true` |

`is_supported()` 是天然的扩展点：新增平台时返回对应判断，并为各平台实现同一接口（`is_enabled` / `set_enabled` / `remove`）。

---

## 7. 验证方法

| 操作 | 预期结果 |
|:---|:---|
| 勾选后保存 | `~/.config/autostart/zen-copyq.desktop` 存在，含 `Hidden=false` |
| 取消勾选后保存 | 文件仍在，但变为 `Hidden=true` |
| 手动删除 desktop 文件后重启应用 | 文件按配置项状态被重新创建 |
| 重新登录桌面环境 | 勾选状态下应用自动启动（托盘图标出现） |
| `desktop-file-validate` 校验 | 生成的 desktop 文件无格式错误 |

---

*撰写：2026-07-30 12:12 (UTC+8) | 依据实现：`src/infrastructure/autostart_manager.py`*
