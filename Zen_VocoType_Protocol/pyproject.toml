[project]
name = "zen-vocotype-protocol"
version = "0.1.0"
description = "Zen_VocoType 通信协议契约库：帧格式、action 常量、错误码、全局路径唯一出处"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zen_vocotype_protocol"]
